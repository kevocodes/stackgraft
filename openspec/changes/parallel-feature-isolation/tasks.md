# Tasks: parallel-feature-isolation

Phase: `sdd-tasks` · Input: `proposal.md` (locked, D1–D10, all seven questions answered plus the correction recorded at Q7) + 7 delta specs (34 requirements, 149 scenarios) + `design.md` (DS33–DS41, **as amended by DS42**) · Next: `sdd-apply`

**DS42 wins wherever it touches DS34 or DS37 as originally written.** The generated target family is three files per store — create, drop, and **read** — because DS34 rung 2 has no other source, and the generated read is held to DS34's discriminator like any other candidate.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~2,080 total — 1a ~140, 1b ~350, 2 ~280, 3 ~380, 4a ~380, 4b ~300, 5 ~250 |
| 400-line budget risk | High for the change as a whole; Low per slice (every slice is inside 400) |
| Chained PRs recommended | Yes |
| Suggested split | PR 1a → 1b → 2 → 3 → 4a → 4b → 5 |
| Delivery strategy | ask-on-risk (`openspec/config.yaml`) |
| Chain strategy | feature-branch-chain (tracker `sdd/parallel-feature-isolation`, matching `portable-multi-stack` D4 and `overlay-reaping`) |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

**Measured slice sizes, recorded as they land, because the forecast is a forecast.** 1a: ~140 forecast. 1b: **~350 forecast, 575 measured** (531 additions, 44 deletions) — over the forecast and over the 400 guideline, and the overrun is reported rather than absorbed. It is where it is because this slice replaces a data model rather than editing one: the schema and its rewritten example are 192 of those lines, and 186 are `check_schema.py`, where the file's own rule — *a check that cannot fail is not a check* — costs one fixture per rule and this slice adds fourteen schema rules, six reading falsifiers and four derivation rows. Two compression passes were applied and are in the number: the schema fixtures were folded to a table and seven simple property blocks to one line each, together −77. The remainder is not compressible without deleting an assertion or making the shipped example unreadable, and neither is worth 175 lines. **Slices 2 and 3 should treat their own forecasts as equally soft**, and 4a in particular, which is forecast at ~380 and ships a script plus two reference files.
3: **~380 forecast, 1101 measured** (1050 additions, 51 deletions) — nearly triple the forecast and nearly double the ~600 slice 2 predicted for it, reported rather than absorbed. **The shipped skill is 185 of the 1101**: `coordination-identity.md` 70, `shared-state.md` 50, the schema 40, the example 17, `discovery.md` 8. The other 912 are the checks, and the reason is one step past slice 2's. Slice 2's properties were readings a grep could not falsify, so it shipped a reader. This slice's are readings **and** a derivation: the name family is an algorithm the skill executes, so proving it means executing it — `verify.sh` gains a generator that takes its prefix, separator, truncation and hashing command **out of the shipped table** and compares against `1.1.0`'s transform frozen beside it, which is ~90 lines before a single row is written. On top of that the three-part proof is three independent conditions, each needing a fixture that fails for **its own** reason and not merely a fixture that refuses (six of them), and the name family needs four. 613 lines land in `verify.sh` (58 rows, 30 of them negatives) and 299 in `check_schema.py` (the identity reader, 8 schema fixtures, the knob-table falsifier). Two compression passes are in the number and the second **added** coverage: merging three sentence loops into one gave all ten sentences a deletion fixture where two had one, at −5 lines. **Slice 4a is forecast at ~380 and ships a script plus two reference files, each of which is executable rather than readable — the forecast to plan against is ~800, not ~380.**
4a: **~380 forecast, 1796 measured** (1777 additions, 19 deletions) — **4.7x the forecast and 2.2x the ~800 slice 3 said to plan for**, reported rather than absorbed, and the fifth forecast in a row to come in low. **The shipped prose is 195 of the 1796**: `isolation-providers.md` 131, `SECURITY.md` 14, `reaping.md` 14, `docs/SHARED-STATE.md` 18, `shared-state.md` 15, `README.md` 3. The other 1601 are the script and the checks, and the reason is one step past slice 3's. Slice 2's properties were readings a grep could not falsify, so it shipped a reader; slice 3's were readings **and** a derivation, so it shipped a generator. **This slice's are readings, a derivation, and a MUTATION OF THE HOST.** Proving that `destroy` removes only what it made, that a refused provision leaves no partial, that an anonymous volume the run created goes with it, and that the base store was not disturbed each require starting and removing real runtime objects and diffing the inventory around them — which no grep and no reader can do. That harness is ~330 of the 923 lines in `verify.sh`, before a single prose row is written, and it costs a fixture base store, a seeded source volume, a scoped inventory snapshot, and a cleanup that is itself asserted. The provider is a further 516: three operations, six refusal paths, two cleanup paths, and a header carrying the measured reasons for each. One compression pass is in the number and it **added** coverage (+14 lines, one more assertion), the same shape slice 3's second pass took. **Slice 4b is forecast at ~300 and inherits this harness rather than rebuilding it — but it adds a discriminator probe that starts a second empty instance per candidate, so plan against ~600, not ~300. Slice 5 is forecast at ~250 and generates three files per store with an approval lifecycle; plan against ~700.**
4b: **~300 forecast, 906 measured** (895 additions, 11 deletions) — **3.0x the original forecast and 1.5x the ~600 slice 4a said to plan for, and the sixth forecast in a row to come in low.** Reported rather than absorbed, and no assertion was compressed to reach a number. **The shipped prose is 73 of the 906**: `isolation-providers.md` 66, `README.md` 4, `traps.md` 2, `SKILL.md` 1 (the body's whole edit). The other 833 are the checks, the schema and the script: **646** in `verify.sh`, **91** in `provider-docker.sh`, **52** in `check_schema.py`, **42** in the schema. **What drove it is one step past slice 4a's, and it is not the discriminator probe alone.** 4a's checks were readings, a derivation and a mutation of the host, and it inherited none of that harness — this slice inherits it and still doubles, because this slice's subject is a *reading over runtime outputs* and every one of those outputs has to be obtained from a real object before the reading can be exercised at all. So the section carries a **second** runtime harness (its own base store, copy, empty instance, scoped leak envelope and asserted cleanup, ~200 lines) because sharing 4a's would entangle two sections' teardowns and delete the per-section leak assertion 4a established; two executable readers with a clause falsifier each; and the C1 extraction that keeps the check from supplying the mechanism it judges — the exec route and the probe's start line read out of the shipped table, and the re-parsing program list read out of `shared-state.md`. **Forty of the 91 rows are negatives**, which is the file's own rule costing one fixture per assertion, and the age alone cost 25 lines of POSIX `awk` date arithmetic because neither obvious helper exists on the supported floor. **Slice 5 is forecast at ~250 and slice 4a said to plan against ~700. Neither number survives this measurement: slice 5 generates three files per store, writes them into a user's repository under an approval lifecycle, carries DS37's two falsifiers and V50's generation half, and bumps four version numbers — every one of which is a mutation of something outside this tree, which is the most expensive thing to check that this change has met. Plan against ~1000, and expect the generated read's own discriminator rows to be the cheapest part of it, because this slice's readers are reusable and are the reason they will be.**
5: **~250 forecast, 1221 measured** (1213 additions, 8 deletions) — **4.9× the original forecast, and 1.22× the ~1000 slice 4b said to plan against, which makes it the first revised forecast in this change to land inside 25%.** Reported rather than absorbed, and no assertion was compressed to reach it. **The shipped skill prose is 51 of the 1221**: `shared-state.md` 48, `isolation-providers.md` 2, `README.md` 1. The rest is **958** in `verify.sh`, **101** in `check_schema.py`, **67** in the schema, **32** in `CHANGELOG.md` and **8** in the four version numbers. **What drove it is the hazard class, not the rule count.** 4a's subject was a mutation of the host and 4b's a reading over runtime outputs; this slice's subject is **a mutation of a repository that is not this one**, and no reading can check it — an appended `Makefile` recipe is invisible to every grep and every reader in the file, so the only instrument that can see one is a hash of the bytes before and after. The fixture repository that makes that checkable — built, committed, written into by a writer carrying four defect modes, four untouchable files digested, an approval fingerprint over three real files, and its own removal asserted — is ~330 of the 958. **The second driver is the one to carry forward: the harness's own false greens cost more than the rules did.** Ten were found by measuring the red run rather than only the green one, and every repair adds lines that assert nothing new — premise gates on the mechanism, positive controls around each mutating half, stand-downs, and six C1 extractions that exist only so a reader cannot supply the shipped rule it is judging. **Slice 6 is a chain close and writes no mechanism; its cost is re-running criteria by hand against an external repository, not lines.**
2: **~280 forecast, 605 measured** (605 additions, 0 deletions) — over again, and for a reason worth carrying forward rather than repeating. The shipped prose is **25 lines of the 605**; the other 580 are the checks, because this slice's requirement makes three properties non-negotiable — the narrowing is one-directional, it cannot relieve a unit's own launch, and it is evidence-bound — and **every one of them is a property of a *reading*, which no grep can falsify.** So the slice ships an executable reader of DS40's five passes (394 lines in `check_schema.py`, including 8 invariant cases and 4 clause falsifiers) beside 22 `verify.sh` rows of which 13 are negatives. One compression pass is in the number (−17). **Slice 3 is forecast at ~380 and is the same shape** — the three-part identity proof and the name family are readings too — so ~600 is the number to plan against, not ~380.

### Decision state

Five items. Items 1 and 2 are decided here and must not be re-decided at implementation. Items 3–5 are carried open, deliberately, with the task that owns each.

1. **DECIDED — slice 4b's acceptance uses a fixture repository carrying a discriminating exec-form healthcheck. The generated read does NOT move forward into 4b.** DS42 leaves this open and forbids discovering it at 4b. Four reasons, in order of weight. **(a) Moving the read forward is circular.** DS42 holds the generated read to DS34's discriminator — so 4b's discriminator must exist before the read can be validated, and the read cannot be 4b's own acceptance evidence for the mechanism that judges it. The fixture breaks that cycle on the correct side. **(b) The fixture is needed either way.** V49's positive — a candidate that *does* discriminate — has no source on the motivating repository (DS42 measured 0 of 4), so a discriminating fixture must be built for 4b whichever way this goes. Moving the read forward does not remove the fixture; it only adds machinery on top of it. **(c) DS37 is one unit and splitting it costs twice.** The read arrives with offer / show / approve / write, the `inferred`-until-observed falsifier and the approval-fingerprint falsifier. Landing one of three files in 4b means both 4b and 5 edit `shared-state.md`'s generation section and both carry V62/V63, which is the two-texts-in-the-tree hazard `overlay-reaping` task 3.4 was opened to close. **(d) It breaks the locked sizes.** 4b is ~300 and 5 is ~250; the generation machinery is most of slice 5, so moving it makes 4b ~450+ (over budget) and hollows 5 out. **The cost is declared rather than absorbed:** at slice 4b, against the 43-service repository this change exists for, 2.0 provisions a copy and then refuses every pair for want of a rung-2 read. Task 4b.1 states that in `README.md` and in `references/isolation-providers.md` as a shipped, temporary state — the same posture slice 1 of `overlay-reaping` took with *nothing reaps yet* — rather than leaving it to be met as a bug.
2. **DECIDED, and it follows from item 1 — slice 5 is NOT optional.** D10's original slice 5 was droppable; DS41 deleted that slice (Q7) and the slice 5 in this plan is a different one. With DS42 measured at 0-of-4 rung-1 coverage, the generated read is **the only rung-2 source on the motivating repository**, so dropping slice 5 ships a 2.0 that copies gigabytes and refuses. No slice publishes and `2.0.0` releases after slice 5; a 2.0 released without it would not meet its own success criteria.
3. **CARRIED OPEN — DS33's donor table and its stated total disagree by one word, and nothing in the tree can adjudicate it.** The nine Δ rows sum to **−35**, not the **−34** the table's own subtotal line states, so slice 1a lands at **484**, not the 485 DS33 records. Spot-checked nine of nine donor rows against the shipped file and every *row* reproduces exactly (Step 1 = 21, Step 2 = 27, Step 3 = 11, Step 4 = 8, Step 8 = 22, Step 9 = 14, Hard Rule 6 = 16, Output bullet 1 = 11, the two Decision Gate rows = 21) — so the rows are right and the subtotal is a transcription error, the identical shape DS33 itself found in DS30 (*"drifted by two words at implementation and nobody noticed"*). **The rows govern; the subtotal does not.** Task 1a.1 re-baselines and task 1a.7 records the **measured** post-edit figure, and `verify.sh` asserts that measured figure rather than either number in this file. Both endpoints satisfy every locked constraint — ≤ 500, net-negative, every intermediate slice at or under the 1a figure — so this blocks no ordering. It is recorded because writing 485 into a check that then measures 484 is how a green suite starts lying.
   **RESOLVED BY MEASUREMENT at slice 1a — the rows governed and the endpoint is 484.** All nine donor rows were reproduced against the shipped file, not three; every cut landed on its stated *To* value; and with all nine applied and no add the body measured **463**, which is `498 − 35` and settles the subtotal. Slice 1a lands at **484**, `verify.sh` asserts that figure as a literal, and slices 1b, 2, 3 and 4a must re-measure 484 rather than 485. Slice 4b's `+3` lands at **487**, not 488.
4. **(b) RESOLVED BY REWORDING at slice 4a, before task 4a.4 edited a byte of the shipped file** (commit `c107f89`) — `specs/isolation-providers/spec.md`'s IP-6 is now *Free space is measured on the filesystem the copy will occupy, and reported as a candidate*. It says in this repository's own words that the check produces **a candidate, not a guarantee**, obliges the run to print the blind spot and name the binding filesystem, forbids *enough space* / *guaranteed* / *reserved* / *will fit*, and adds the mid-copy exhaustion that leaves the object inventory as it found it. **A measurement corrected the design beside it:** DS36's stated `df -P` is 512-byte blocks on the host and 1024 inside a container, so the shipped sentence would have read the host as twice as free as it is — the permissive direction. Both sides use `df -Pk` and `verify.sh` fails the script if a bare `df -P` reappears.
4. **(a) RESOLVED at slice 3, before task 3.3 edited a byte of the shipped file** — `specs/shared-state-safety/spec.md`'s Verify clause now reads *the In-instance isolation column of the per-substrate table is gone while the table and its catch column remain*, with the reason recorded beside it: the four refuse-cases that rest on the catch column are named, and a reader who deletes the table deletes their evidence. **(b) stays open for slice 4a.** Original text follows.
4. **CARRIED OPEN — two locked spec sentences must be corrected before the slices that implement them, and they are spec edits, not code.** `design.md`'s *Locked decisions this design cannot deliver as written* names both. (a) `shared-state-safety`'s Verify clause says *the substrate namespace table is gone*, which taken literally deletes the evidence four of the six no-safe-answer cases rest on; **what must be deleted is the In-instance isolation column, not the table** — narrow the sentence before slice 3 (task 3.3). (b) `isolation-providers`' IP-5 says a copy that would not fit is *refused before it starts rather than failed during it*, which no check can deliver across concurrent worktrees; the honest wording is this repository's own — **a candidate, not a guarantee** — reworded before slice 4a (task 4a.4). Neither is resolved silently here.
5. **CARRIED OPEN — two success criteria in `proposal.md` cannot be closed as written at 2.0, and one verification baseline is not reproducible in CI.** (a) *"Each of F1–F5 has a named regression case that fails against 1.1.0 and passes at 2.0"* — **F3 is D9, which Q7 puts outside 2.0**, so F3 has no regression case in this change and the criterion closes for F1, F2, F4, F5 only. (b) The 116/156 refusal baseline and the 39/4/156 derivation counts require the external 43-service repository, which is not in this tree and cannot be a CI row; they are recorded observations run by hand at the chain close (task 6.1), never asserted green by the suite. Task 6.3 states both rather than ticking them.

### Verification legend

`openspec/config.yaml` `available_verification` carries the five methods established by `portable-multi-stack`:
**V1** SKILL.md style-guide review (incl. the body word counter) · **V2** JSON well-formedness + JSON Schema validity of `assets/*.json` · **V3** cross-file: every field named in SKILL.md/`references/` exists in the schema (`check_schema.py`) · **V4** portability grep · **V5** `dash -n` + one `command -v` per name on macOS and a minimal Linux image.

**V6–V33** are `overlay-reaping`'s shipped rows in `.github/scripts/verify.sh` and are carried forward unchanged. **V5 auto-covers `provider-docker.sh`** — `verify.sh:26` globs `"$SKILL"/scripts/*.sh`, so the new script joins the `dash -n` + shebang loop the moment it exists.

**V34–V69 are new.** Every one keeps the file's rule — **a check that cannot fail is not a check** — so each positive carries a negative fixture that must be REJECTED. The last audit found twenty-two rows that could not fail; four of them were rows whose negative control exercised a *different* condition than the row asserted (`overlay-reaping` W4, C3, C3-R, A10), so each negative below names the condition it must exercise, not merely a non-zero exit.

| # | Positive assertion | Paired negative (must be REJECTED) | Slice |
|---|---|---|---|
| V34 | `body_words` reports ≤ 500 and equals the slice's **measured** recorded figure, re-read from `verify.sh`'s own `body is <N> words` line and never from a design table | a fixture body at exactly 501 fails; and the recorded figure is asserted as a literal, so a slice landing at a different count fails rather than passing under a `-le 500` that would have accepted anything | 1a, 1b, 2, 3, 4a, 4b, 5 |
| V35 | The over-budget fixtures are **derived**: `over.md` = `501 - $(body_words "$SKILL/SKILL.md")` filler words, landing at exactly 501 — one word over | three negatives: (a) the shipped hard-coded `38` reintroduced measures 36 words clear of the ceiling and fails the calibration row, because a counter miscounting by up to 35 would still report as working; (b) `adds-first.md` = `over_n + <the slice's recorded add count>`, and the row fails unless it measures **strictly above** `over.md`; (c) a fixture body under the ceiling must be ACCEPTED, so the calibration row is not passing on a fixture that fails for being a fixture | 1a |
| V36 | `description` is one quoted physical line of ≤ 250 characters, measured by `desc_measure()` modelled on `compat_measure` | five fixtures: deleted, unquoted, embedded quote, 251 rejected, **and exactly 250 ACCEPTED** — the requirement is *at most* 250, the `-le` boundary A8 fixed for `compatibility`. Plus: `desc_measure` must state its unit and reject a value it cannot measure in that unit, because `compat_measure` counts **bytes** while this requirement says **characters**, and a ceiling stated in one unit and checked in another is DS33's own defect one field along. **This check does not exist today and this change edits the field.** | 1a |
| V37 | `compat_measure` ≤ 500 with the store-copy clause present | the clause kept while its donor cut is reverted must fail — the DS32 fixture shape, derived from the shipped value and standing down loudly when that value is unmeasurable | 1a |
| V38 | The scope and the non-goals appear in **all three** places D1 names: `description`, the Activation Contract, `README.md` | four fixtures, every one of which must FAIL: the scope deleted from each of the three in turn — silence about scope is what the whole change descends from — plus a portability-shaped grep catching a claim of universal applicability | 1a |
| V39 | The body contains no `REUSE`, `ISOLATE`, `REAP`, and no verdict term this change introduces | the case-sensitive loop fires on a fixture body line naming each, the new terms included | 1a, 4b |
| V40 | The body names neither `isolation-providers` nor `coordination-identity`, and carries no hazard-to-mechanism wording | a fixture body carrying a Decision Gate row whose action cell reads *Copy it* is caught — naming both files **is** the mapping the gate spec forbids the body to carry | 1a, 4b |
| V41 | `provider-docker.sh` parses under `dash -n`, carries a shebang, and is in the named script inventory | the inventory notices it deleted, as it already does for `reap.sh` | 4a |
| V42 | The provider and both new `references/` files name no `jq`, `python3`, `sha256sum`, `flock`, `timeout`, `lsof`, `ss`, and no GNU-only construct | `$PORTABILITY` is case-insensitive and intent-blind, so a fixture naming one **inside a comment or inside a negative fixture's own prose** must fail; `$GNUISM` fixture likewise | 3, 4a, 4b |
| V43 | `provision` → `address` → `destroy` leaves the **host filesystem** byte-identical outside the runtime objects it made | a fixture provider writing one file beside the destination is caught by `unreported_debris`, with the lock directory's lesson applied: the allowance takes a set of basenames it may never excuse | 4a |
| V44 | `destroy` removes only an object whose `stackgraft.worktree` label **equals** the worktree argument, normalised on both sides per DS27 | three negatives, each still existing afterwards: the same copy with a *different* worktree argument; an unlabelled volume; a volume whose `stackgraft.labels` value this run does not recognise | 4a |
| V45 | A provision that runs out of space exits 3 and leaves **no partial volume** | the runtime's volume list is compared before and after and must be identical — an exit code alone cannot see a surviving partial, which is the shape `overlay-reaping` W1 shipped and had to correct | 4a |
| V46 | The space check interrogates the runtime's data root **and** the host filesystem backing it, and names which one bound the decision | two negatives: a probe that cannot answer for either ⇒ refuse (unknown is not a permission); and a check reading only the working directory's filesystem must FAIL the row | 4a |
| V47 | DS36: the run reports a **candidate**, printing the filesystem it measured, this repository's copies already on it, and the blind spot it cannot see | three fixtures, every one of which must FAIL: (a) wording that promises a guarantee — `enough space`, `guaranteed`, `will fit`, `reserved`; (b) a report with the blind-spot statement absent, since silence reads as completeness; (c) a report asserting a host-global total, which no run can produce | 4a |
| V48 | An **exec-form** `healthcheck.test` is harvested as a verification candidate | a `CMD-SHELL` healthcheck must **not** be, and the refusal names the shell form — the template contract's argv and no-re-parse rules applied unchanged | 4b |
| V49 | DS34's discriminator: a candidate whose output **differs** against an empty instance of the same image is recorded as a query, and the copy's output matches the base's byte for byte | two shipped negatives, both measured to fail it: `pg_isready` and `redis-cli ping` answer identically on an empty instance and are **rejected as a query**, and the pair refuses. Third negative: a candidate admitted without the probe having run at all must fail the row | 4b |
| V50 | DS42: the generated **read** is held to the same discriminator, and a schema-agnostic counting read — zero on an empty instance, non-zero on a seeded one — is accepted | a generated `SELECT 1` must be **rejected exactly as `pg_isready` is**; and a generated read admitted because the skill wrote it must fail, which is DS37's `declared`-on-write defect reaching through the verification door | 4b probe · 5 generation |
| V51 | A copy seeded from a good volume matches the base's readback and counts as isolated | a copy from a deliberately truncated volume mismatches ⇒ the copy is destroyed, the pair refuses, **and the overlay's peer configuration is inspected to prove it was not silently wired to the base store** — the fall back is the contamination the gate exists to prevent | 4b |
| V52 | The base store's container uptime, restart count and open connections are unchanged across a copy | a fixture that stops the base store must FAIL this row, so the row is shown able to report a disturbance rather than passing on a copy that disturbed nothing because nothing happened | 4a |
| V53 | Two launches from one worktree reuse one instance identity, and the age line is printed | a run that provisions nothing, refreshes nothing and destroys nothing **still** prints the age — its absence fails the row; and a refresh triggered by elapsed time alone must fail, since no heuristic may refresh or refuse on age | 4b |
| V54 | No shipped file states an expected, typical, or worst-case duration | a fixture line `typically 51 s` is caught, and a cited measurement not labelled as one sample is caught | 4a |
| V55 | No shipped file states a data age beside a copy | fixture lines for `up to date as of`, `stale`, `fresh` and `data is N old` must each be caught — all four are comparisons the run did not perform | 4b |
| V56 | The SQL form is byte-identical to `1.1.0`'s for a fixed branch; the label form carries no `_`, is ≤ 63 characters, and is alphanumeric at both ends | two negatives: two branches whose slugs truncate identically must differ in **both** forms, because `hash8` is taken over the untruncated branch name; and a generator that post-processes `_`→`-` must fail the byte-identity row, that post-processing being where F1 came from | 3 |
| V57 | No shipped file describes an allocation, a slot pool, an exhaustion case, or a release obligation for a namespace name | a fixture naming `16 logical databases, allocated per worktree` is caught; and the closed placeholder set is greped for a placeholder yielding an allocated value, finding none | 3 |
| V58 | A frontend-only diff against a multi-store repository gates zero pairs and names each removed pair **with the record that removed it** | three negatives: the same diff against a unit recorded as migrating re-inserts that pair with W=yes and names the trigger; a narrowing on an absent record must refuse; a narrowing on a record whose `serviceFingerprint` has drifted must refuse | 2 |
| V59 | A manifest with a determinacy record for one store and **none** for another validates, and each pair classifies separately | a manifest carrying a service-level `writes` array is rejected by the schema; and a record for `(U, postgres)` read as evidence about `(U, kafka)` or `(billing, postgres)` must fail | 1b |
| V60 | `references/shared-state.md` states the `dependsOn` prohibition **inside** the narrowing rule, in those words | a copy of that file with the prohibition sentence deleted must FAIL the row — this is what proves the check reads the sentence rather than confirming the file exists, the C1 lesson that a link check proves a file exists and nothing more | 2 |
| V61 | Both new `references/` files resolve under the backtick link loop and pass `check_schema.py`'s document→schema cross-check | its existing `notAManifestField` self-test proves the cross-check can fail; add a fixture backticking a camelCase non-field in **each** new file and confirm both FAIL. The link loop's pattern is `` `\(references\|assets\|scripts\)/[a-z.-]*` `` — grepping for the literal string misses it | 2, 3, 4a, 4b |
| V62 | DS37: a generated target is recorded `inferred` before first use and `declared` only after a run has observed create, drop **and read** succeed, with timestamp and exit status | two negatives: a target written and never run must **not** satisfy the gate; and a teardown executed before any create has been observed must be refused with the namespace and the manual command named | 5 |
| V63 | DS37's second falsifier: any later edit to an approved file — the skill's own included — drops the approval and re-shows the template | three negatives: an edited file whose approval survives fails the row; a generated drop aimed at a name not derived from the name family is rejected by the destructive-verb class, judged by effect; and a run that staged, committed, or pushed the generated files fails | 5 |
| V64 | A pair carrying X and not W leaves the runtime's volume **and** instance inventory unchanged, and is resolved by identity | the same inventory diff for a W-and-X pair must show a provision — proving the row can see one, rather than passing because nothing ever provisions in the fixture; plus an identity recorded but not delivered into the overlay's environment leaves X undetermined and refuses | 3 |
| V65 | Managed, remote, host-native and undeterminable-locality stores each refuse **by name**, with discovery's recorded reason carried into the message | a unit paired with one refused store and one provisionable store: the refusal must not cascade, the local pair still resolves to a verified copy — proving the refusal is scoped and not a blanket stop | 4a |
| V66 | Every copy carries this repository's **complete** label set and is named in the output with its exact removal command, including on a run that removed nothing | three negatives: a copy with a partial label set is reachable by no reap flag combination; an unlabelled volume holding data survives both flags unchanged; and a runtime that cannot be queried reports **unknown**, never zero copies | 4a |
| V67 | The three derivation counts are reported (runnable units, `backingStores` entries, pairs) and a narrowed count appears **beside** the derived one | a report that replaces the derived count with the narrowed one must FAIL — a narrowing must not be readable as a smaller repository; and two runs at the same commit with the same manifest state must report identical counts | 1b, 2 |
| V68 | `schemaVersion` is `3`, exactly one transition occurs across the seven slices, and a schema-2 manifest is discarded whole with nothing carried over | a manifest containing `writes`, `healthPath`, `dockerfile`, or `kind: "static"` is rejected; and a second transition anywhere in the chain fails the row | 1b |
| V69 | The four version numbers agree at `2.0.0` with a breaking `CHANGELOG.md` entry | the existing partial-bump fixtures — one number raised and the others left | 5 |

### Suggested Work Units

| Unit | Goal | PR (base) | Focused check | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1a | Scope and body budget: `description`, `compatibility`, Activation Contract, the DS33 cuts and adds in one commit, README/docs, `desc_measure()` and the recalibrated body fixtures | PR 1a (base: tracker `sdd/parallel-feature-isolation`) | V1 + V34 + V35 + V36 + V37 + V38 + V39 + V40 | `sh .github/scripts/verify.sh` on macOS and `alpine/git`; the three counters run by hand before and after the single body commit | The SKILL.md frontmatter and body diff plus the README/docs scope paragraphs. Reverting leaves `1.1.0` behaviour intact; nothing functional depends on this slice |
| 1b | Per-store determinacy: `schemaVersion` 3, records replace `writes`, `migrates` scoped, example rewritten, discovery's classification pass | PR 1b (base: PR 1a) | V2 + V3 + V59 + V67 + V68 + V61 | A manifest fixture set (one store recorded, one omitted; `writes` present; retired fields); a schema-2 manifest loaded and confirmed discarded | `assets/*.json` + the `discovery.md`/`shared-state.md` determinacy sections. Reverting restores schema 2; caches written at 3 are discarded by the fail-safe that already exists |
| 2 | Change-scoped gating: the narrowing rule with its evidence obligation and the `dependsOn` prohibition inline, the five-pass order, the reported counts | PR 2 (base: PR 1b) | V58 + V60 + V67 + V61 | A frontend-only diff against a multi-store fixture repository; the same diff against a unit recorded as migrating; a drifted-record run | The `shared-state.md` narrowing section + `traps.md`'s under-reporting trap. Reverting returns the gate to the full derived subject, which over-refuses rather than under-gates |
| 3 | Coordination identity: `references/coordination-identity.md`, the knob rows moved, the name family, the allocator deleted, the closed placeholder set updated | PR 3 (base: PR 2) | V56 + V57 + V64 + V42 + V61 | Two branches whose slugs truncate identically, both forms generated; a bucket created under the label form against a real object store; an X-only pair with the runtime inventory diffed | `references/coordination-identity.md` + the schema's placeholder set + the deleted column. Reverting restores `{{isolationName}}`, which is F1 again — so this slice does not revert cleanly past 1b |
| 4a | Provider and copies: `scripts/provider-docker.sh`, `references/isolation-providers.md`, free space, labels, reap integration, `SECURITY.md`, remote refusals | PR 4a (base: PR 3) | V5 + V41 + V42 + V43 + V44 + V45 + V46 + V47 + V52 + V54 + V65 + V66 | The provider run through provision-address-destroy on macOS and `alpine/git`; a filled filesystem; the base store inspected for uptime/restarts/connections across a copy; an unlabelled volume | `scripts/provider-docker.sh` + `references/isolation-providers.md` + the reaping and SECURITY additions. Copies already provisioned survive a revert as labelled volumes, removable with the documented command |
| 4b | Verification and lifetime: the readback and its discriminator, copy lifetime and age, refusals by name, Output Contract bullet 5 | PR 4b (base: PR 4a) | V48 + V49 + V50 + V51 + V53 + V55 + V34 + V39 + V40 | A **fixture repository carrying a discriminating exec-form healthcheck** (decision-state item 1); `pg_isready` and `redis-cli ping` as the shipped negatives; a truncated-volume copy; two launches from one worktree | The verification and age sections of `isolation-providers.md` + the one-word-triple body edit. Reverting leaves 4a's copies unverified, which is why 4a alone must not be released |
| 5 | Generated lifecycle target and release: offer / show / approve / write for **three** files per store, `inferred` until observed, inert teardown, the four version numbers | PR 5 (base: PR 4b) | V50 + V62 + V63 + V69 + V34 | A repository with a discoverable store and no target, exercised through offer, decline and approval; a create that fails on first use; an approved file edited afterwards | The generation section of `shared-state.md` + the version bump. Files already written into a user's repository are **theirs** and are not removed by a revert — stated in the release notes |

**Body word ceilings — the binding constraint is `498 + adds − cuts ≤ 500` at every commit, not only at every slice boundary.** DS33's `adds-first` hazard is live here: 498 + 21 = **519**, red at that commit, which is exactly why task 1a.7 is one commit and why V35 keeps a fixture reproducing it. Baseline **498** by `body_words`; slice 1a is net **−14**, measured, ⇒ **484** (decision-state item 3, resolved: the rows summed to −35 and the subtotal's −34 was the transcription error); slices 1b, 2, 3 and 4a change no body word and must re-measure **484**; slice 4b is **+3** ⇒ **487**; slice 5 touches `version` and `metadata.version`, which the counter does not see. Net **−11** against 498, thirteen words of ceiling headroom, and no intermediate above the 1a figure. **Every slice re-runs the counter and asserts the measured number; no slice trusts this paragraph.**

**`description` has no check at all today**, unlike `compatibility`'s five fixtures, and this change edits it — so task 1a.3 lands `desc_measure()` and its five fixtures **before** task 1a.5 edits the field. Measuring a field with a guard that cannot fail is the defect A8 spent a whole amendment repairing, and doing it in the other order repeats it knowingly.

---

## Phase 1a: Scope and body budget (PR 1a — base `sdd/parallel-feature-isolation`)

- [x] 1a.1 **[RED]** Re-baseline all three counters before touching anything and record the observed numbers here: `awk 'f{n+=NF} /^---$/{c++; if(c==2) f=1} END{print n}' skills/stackgraft/SKILL.md`, `compat_measure`, and the `description`'s length by hand (no guard exists yet). If the body is not **498**, re-derive DS33's donor table and the 485/488 targets before any edit — every later task in all seven slices is arithmetic against this number. Also spot-check at least three donor rows against the shipped file, because DS33's Δ column sums to −35 against a stated −34 (decision-state item 3) and the rows are what governs. *Verify: V34; V36; V37 — recorded as observed values, never as expectations.*
  - **Done.** Measured on `d71589e`, before any edit. Body **498** (`awk 'f{n+=NF} /^---$/{c++; if(c==2) f=1} END{print n}'`), matching DS33's section table exactly (27/163/88/150/51/19). `compat_measure` **477** bytes. `description` **157** characters — DS33 says ≈155, so the forecast was two characters light. **All nine donor rows spot-checked, not three**, and every one reproduces its stated *From*: Activation Contract 27, Step 1 21, Step 2 27, Step 3 11, Step 4 8, Step 8 22, Step 9 14, Hard Rule 6 16, Output bullet 1 11, the two gate rows 11+10=21. **The rows sum to −35 and the subtotal's −34 is the transcription error**, so the slice lands at **484**, not 485. Confirmed by construction at 1a.7: the body measured 463 with all nine cuts applied and no add. Baseline suite: 188 ok, 0 FAIL, exit 0.
- [x] 1a.2 In `openspec/config.yaml`: extend `available_verification` with this change's harness — the `description` counter as a named command, the discriminator probe against an empty instance of the same image, the two-filesystem space probe, a fixture repository carrying a discriminating exec-form healthcheck, a truncated-volume copy, and the runtime volume/instance inventory diff. In the same edit correct `rules.apply.guidelines`' `180-450 tokens; recommended max 700, hard max 1000` to the unit the shipped counter measures — **words, at most 500** — per DS33: a token count is unmeasurable by anything this repository is allowed to ship, and the current recommendation is already violated by the shipped body. *Verify: file review — no listed method covers `config.yaml` itself, the same flag `portable-multi-stack` task 1.1 and `overlay-reaping` task 1.2 both raised.*
  - **Done.** `rules.apply.guidelines` corrected from `180-450 tokens; recommended max 700, hard max 1000` to at most 500 words, with the reason the unit is words. Six entries added to `available_verification`: the `desc_measure()` counter with its unit discipline and six fixtures, the discriminator probe, the two-filesystem space probe, the discriminating-healthcheck fixture repository, the truncated-volume copy, and the runtime inventory diff. **Observation for a later slice, not fixed here:** the shipped `available_verification` budget entry still names `awk -F'"' '/^compatibility:/{print length($2)}'`, which is the counter DS32 replaced because it reports 0 for an absent field — it is stale against `compat_measure`, and outside this task's scope.
- [x] 1a.3 **[RED]** Add `desc_measure()` to `.github/scripts/verify.sh`, modelled on `compat_measure` and sharing one `desc_verdict()` with every fixture so the fixtures exercise the shipped guard rather than a second copy of it. Distinguish **absent / unquoted / embedded-quote** from a small number, exactly as `compat_measure` does. State the unit in the row text: the requirement says 250 **characters** and `compat_measure` counts **bytes**, so `desc_measure` must either count characters or reject a value it cannot measure in the declared unit — a ceiling stated in one unit and checked in another is DS33's own defect one field along. Boundary is `-le 250`. **Lands before 1a.5.** *Verify: V36 with all five fixtures — deleted, unquoted, embedded quote, 251 rejected, and exactly 250 accepted.*
  - **Done.** RED first, with real output: `rg 'description' .github/scripts/verify.sh` matched nothing, and the full suite exited **0 with 190 ok and 0 FAIL** over a planted 251-character `description`. GREEN: `desc_measure()`/`desc_verdict()`/`desc_fixture()` land, sharing one verdict with every fixture. **The unit question is answered by refusing rather than by guessing:** POSIX `awk`'s `length()` is bytes under `LC_ALL=C` and characters under UTF-8, so the guard measures in the C locale and returns `non-ascii` for any value it cannot report in characters. **Six fixtures, not five** — deleted, unquoted, embedded quote, 251 rejected, exactly 250 accepted, and the multi-byte value refused as unmeasurable. Re-run against the same planted 251: `FAIL description is unmeasurable or over the 250 ceiling: 251`.
- [x] 1a.4 **[RED]** Recalibrate `verify.sh`'s two body fixtures. `body_fixture "$bf/over.md" 38` was `501 - 463`, one word over `overlay-reaping`'s slice-1 forecast; against the real 498-word body it produces 536, clearing the ceiling by 36, so a counter miscounting by up to 35 would still be reported as working — the fixture has stopped being a boundary test and CI is green over the drift. Derive it: `over_n = 501 - $(body_words "$SKILL/SKILL.md")`. Derive `adds-first.md` as `over_n + <this slice's recorded add count>` and add a row asserting it measures **strictly above** `over.md`, standing down loudly if it does not, the pattern the donor-cut fixture already uses. *Verify: V35 with all three negatives; V34.*
  - **Done.** RED first: with the shipped hard-coded 38 the new calibration row printed `FAIL the over-ceiling fixture measured 536 words, not the 501 a boundary test sits at` — 36 words clear of the ceiling, exactly as forecast. GREEN: `over_n = 501 - $(body_words "$SKILL/SKILL.md")`, and `adds-first.md` = `over_n + BODY_ADDS` with `BODY_ADDS=21`. Three negatives, all green: the reintroduced hard-code (reports its margin, since `body_verdict` already said `fail` for all 36 of those words), adds-first asserted **strictly above** over.md, and an under-ceiling fixture of the same shape that must be ACCEPTED. At 484 the derivation and the old hard-code coincide at 38 filler words; that is arithmetic, not redundancy, and the comment says so.
- [x] 1a.5 **`description` — one edit.** Append the scope clause: ` Local development: one host, one base stack, N worktrees.` Measured ≈155 → ≈212 characters against the 250 ceiling, **measured by 1a.3's guard and never assumed**. Trigger words stay first; it stays one physical quoted line. **Depends on 1a.3.** *Verify: V36; V38.*
  - **Done.** ` Local development: one host, one base stack, N worktrees.` appended. **Measured by 1a.3's guard: 157 → 215 characters** (DS33 forecast ≈212 from a ≈155 baseline; both were two characters light). 35 characters of headroom against 250. One physical quoted line, trigger words first, pure ASCII.
- [x] 1a.6 **`compatibility` — one edit, cut and clause together**, the DS32 discipline: `…only for container repos.` → `…only for container repos and store copies.` (+17 bytes) against a measured ≈477 of 500. If the measured value leaves fewer than 17 bytes of headroom, the named donor is `all CI-tested. ` (−14) in the first sentence, applied in the **same** edit — a clause appended and then paid for is red at that commit. One physical line, no embedded `"`, measured in bytes rather than assumed in characters. *Verify: V37; V1.*
  - **Done.** `…only for container repos.` → `…only for container repos and store copies.` **Measured 477 → 494 bytes**, exactly the +17 forecast. **The conditional donor did not fire and was not applied:** the measured value left 23 bytes of headroom, more than the 17 the clause costs, so `all CI-tested. ` stays. V37 gains a presence row — `compat_measure ≤ 500` is equally satisfied by a line the edit never reached — with the reverted line as its negative.
- [x] 1a.7 **SKILL.md body — one commit, cuts and adds together.** Apply DS33's donor table verbatim: Activation Contract gains the 21-word scope line `Local development only: one host, one running base stack, N worktrees. CI, shared or remote hosts and multi-developer stacks are non-goals.` (+21); Step 1 drops *never this checkout* 21→17; Step 2 recast 27→20 (the fingerprint recipe is verbatim in `discovery.md` §5 step 1); Step 3 *overlays of this repository* → *this repository's overlays* 11→10; Step 4 drops the duplicate pointer 8→7; Step 8 drops the exclusion enumeration 22→18; Step 9 drops *rewiring unchanged dependencies…* 14→7 (duplicates Hard Rule 1); Hard Rule 6 drops *refresh drifted entries* 16→13; Output bullet 1 *name refreshed entries* → *naming which* 11→10; the reaping and stopping Decision Gate rows merge to the 14-word `| Overlay outlived its worktree | Report it — references/reaping.md; stop nothing unproven |`. **Applying the add before the cuts puts the body at 519 and CI is red at that commit** — which is what `adds-first.md` exists to reproduce. Record the **measured** result; per decision-state item 3 it is 485 or 484 and the rows govern, not DS33's subtotal. Hard Rule 8 is not a donor and is not touched. *Verify: V34 (the measured figure, asserted as a literal); V35; V39; V40; V1.*
  - **Done, one commit.** Every cut lands before the add: **498 → 463 (nine cuts) → 484 (+21)**, so no intermediate state exceeds 500. Each cut hit its target exactly, measured: Step 1 17, Step 2 20, Step 3 10, Step 4 7, Step 8 18, Step 9 7, Hard Rule 6 13, Output bullet 1 10, merged gate row 14, Activation Contract 27 → 48. **The measured result is 484, not 485** — decision-state item 3 resolved in favour of the rows. Step 8's wording is `one excluded port per argument` rather than a bare deletion, because dropping the enumeration outright lands at 16 and the row's target is 18. `verify.sh` asserts 484 as a literal with a one-word drift (485, legal under the ceiling) as its negative. Hard Rule 8 untouched.
- [x] 1a.8 In `README.md` and `docs/HOW-IT-WORKS.md`: the same scope and the same non-goals in the opening a prospective user meets, stated as a boundary rather than softened into a preference — a user on a shared or CI host must be told there, not through a refusal later. *Verify: V38.*
  - **Done.** README.md carries the scope and the four non-goals in the opening a reader meets, before the quick start, as a boundary — *if your base stack does not run on the machine you are sitting at, stackgraft is the wrong tool*. docs/HOW-IT-WORKS.md states the same boundary before the four ideas, which are only correct inside it. Both put every claim on one physical line, which is what V38 requires.
- [x] 1a.9 In `CONTRIBUTING.md`: correct the budget paragraph to the measured number and the unit the shipped counter measures, so the three files that state this budget stop disagreeing. *Verify: V34; file review against `openspec/specs/portable-runtime/spec.md`'s corrected unit.*
  - **Done.** The budget paragraph now states **at most 500 words** — the unit the counter measures, never tokens, with the reason — records the measured **484** and its sixteen words of headroom, and says `verify.sh` asserts that number as a literal rather than merely under 500. Agrees with the delta spec's corrected unit in `specs/portable-runtime/spec.md`; `openspec/specs/portable-runtime/spec.md` still says 700 tokens and is corrected at archive, not here.
- [x] 1a.10 Extend `.github/scripts/verify.sh` with V38, V39 and V40 and their negatives, keeping the file's discipline — no row lands without a fixture that must be REJECTED. V39 extends the existing case-sensitive `REUSE`/`ISOLATE`/`REAP` loop; V40 is a new grep over the body for the two new reference filenames and for hazard-to-mechanism action cells. *Verify: run `sh .github/scripts/verify.sh` on macOS and `alpine/git`; each new positive demonstrably fails when its negative fixture is substituted.*
  - **Done.** RED first: the full suite exited **0** with the README scope paragraph deleted. GREEN: V38 asserts the scope in all three homes, with both tests demanding **one line carrying every claim** — `CI`, `shared` and `remote` each occur in these files for unrelated reasons, so four independent greps would report a declared scope over a file that declares nothing. Four negatives, each demonstrated failing against the shipped tree: scope removed from `description` (`FAIL description does not state the scope`), from the Activation Contract (`FAIL the Activation Contract is missing the scope or a non-goal`), from README.md (`FAIL README.md is missing the scope or a non-goal`), and a universality claim (`FAIL a shipped file claims universal applicability: README.md:130:It works with any stack.`). The universality pattern is narrow enough not to fire on shipped text about something else — `no matter what \`writes\` says`, `any host running this probe`. V39: `COPY` joins `VERDICT_TERMS` and the negative now names both REAP and COPY, asserting **both** are found. V40: a new case-insensitive grep over the body for the two new reference filenames and for hazard-to-mechanism action cells, with a gate row reading `Copy it — references/isolation-providers.md` as its negative.
- [x] 1a.11 Update `README.md` Status: the scope is now declared and **nothing about isolation has changed yet** — schema 2, `writes` still present, ISOLATE still means an in-instance namespace. State it plainly so slice 1a does not read as a shipped feature. *Verify: file review — no claim about copies, providers, or per-store determinacy.*
  - **Done.** A first bullet under *Honest limits* — the section this repository uses for exactly this — states that declaring the scope is the whole of this step: `schemaVersion` is still 2, determinacy is still one `writes` array per service, ISOLATE still means an in-instance namespace, no copy is made, no provider ships, and no refusal behaves differently. No claim about copies, providers or per-store determinacy.

## Phase 1b: Per-store determinacy (PR 1b — base PR 1a)

- [x] 1b.1 **[RED]** Write the schema fixtures into `.github/scripts/verify.sh` **before** the schema changes: a manifest carrying a service-level `writes` array must be REJECTED; a manifest with a determinacy record for one store and none for another must VALIDATE; `healthPath`, `dockerfile` and `kind: "static"` must be rejected; a record for `(U, postgres)` must not be readable as evidence about `(U, kafka)` or `(billing, postgres)`. Record that each currently fails against schema 2. *Verify: V59; V68 — all RED at this task.*
  - **Done, RED demonstrated with real output against the shipped schema 2 before a byte of it changed.** The JSON-level fixtures land in `check_schema.py` — the home V2/V3 already use, invoked from `verify.sh` with the established loud skip — and the version, vocabulary and prose rows land in `verify.sh`'s new `determinacy` section. RED, verbatim: `FAIL ACCEPTED but must be rejected: a manifest still written at schemaVersion 2`; `FAIL ACCEPTED but must be rejected: a service-level writes array`; `FAIL ACCEPTED but must be rejected: migrates as the unit-level boolean it used to be`; five record-shape fixtures reporting `rejected by the wrong rule … got ['/properties/services/additionalProperties/additionalProperties']`, which is the generic catch-all standing in for rules that did not exist; the per-pair reading answering `read undetermined, wanted W=True X=False`; and in `verify.sh` `FAIL the schema declares schemaVersion '2', not 3`, `FAIL shared-state.md is missing 3 of the three determinacy sentences`, `FAIL 18 line(s) present writes as a current field`.
  - **A false green was found and repaired at this task, which is the standard slice 1a set.** The omission row first read `awk '/omit/ && /checked-and-none/'` and was **green against the shipped file before this slice wrote anything** — `references/discovery.md`'s `dependsOn` bullet already carries *omitting* beside *checked-and-none* for an unrelated reason, so the row reported a stated rule over a file that stated nothing. Tightened to `/omitted record/`, which is RED against the same file and green only once the rule is written.
  - **`healthPath`, `dockerfile` and `kind: "static"` were already rejected under schema 2** and are recorded as such rather than claimed as new coverage: the first two by `additionalProperties: false`, the third by its own `not`. Only `writes` needed a named rule, and it gets one (`"not": {}`) so the failure says why the field is gone instead of reading as a typo.
- [x] 1b.2 In `assets/manifest.schema.json`: `schemaVersion` const `2` → `3`. Add one determinacy record per `(unit, store)` carrying W, X, the evidence for each, its confidence, and its own `serviceFingerprint`. **Remove `writes` entirely** — and add no field whose *presence* asserts checked-and-none for every store at once, because that reading is the defect. Recast `migrates` as the store names the entrypoint is pointed at, never a unit-level boolean. Retain `fingerprint`, `fingerprintTool`, `runnable`, `backingStores`, `competesOn`, `acceptedRisks`, `portPolicy.ranges` keyed by `portGroup`, and `additionalProperties: false`. **No migration path is written, and its absence is the design.** *Verify: V2; V3; V59; V68.*
  - **Done.** `schemaVersion` const `2` → `3`, declared once. `determinacy` is a map under each unit keyed by the `backingStores` key, so *one record, one pair* is structural rather than procedural — no `<unit>::<store>` split to get wrong, and the `acceptedRisks` comment's own warning about that split does not have to be repeated. Each record requires `at`, `method`, `confidence`, `serviceFingerprint`, `mutates` and `competes`; each boolean is `{value, evidence}` with **both required and `evidence` at `minLength: 1`**, because a record that can say *none for this store* is a record that can say it without looking. **Both booleans are required**, so a pass that answered W and could not answer X writes no record and the pair stays undetermined rather than half-examined. `writes` is retired **by name** with `"not": {}` so the rejection carries the reason; measured, a `false` property schema reports its error path as `…/properties` with the property name dropped, which is why `not` was used where the rule has to be nameable. `migrates` is an object with a required `pointing` discriminator: `recorded` obliges `stores` (`minItems: 1`), `unknown` **forbids** `stores` — the two states must stay distinguishable or an unscoped migration is byte-identical to no migration at all. `runnable: false` now also forbids `determinacy` and `migrates`, which makes TD-2's *a non-runnable unit contributes no pairs* structural. `fingerprint`, `fingerprintTool`, `runnable`, `backingStores`, `competesOn`, `acceptedRisks`, `portPolicy.ranges` by `portGroup` and `additionalProperties: false` all retained; no migration path written.
  - **Addition beyond the task list, recorded rather than absorbed: `backingStores[].locality` (`{value, reason}`, both required).** Task 1b.4 says to *record* each store's provider eligibility with its reason, and `additionalProperties: false` means an unlisted field cannot be written — so the instruction had no home and would have shipped unimplementable. `value` is the five-way enum, `reason` carries the fact it rests on. It does **not** overlap task 4a.7, which extends `isolation`; slice 4a reads this field rather than re-adding one.
- [x] 1b.3 Rewrite `assets/manifest.example.json` whole against schema 3, and make the example itself demonstrate the granularity: one unit with a record for one store and **no record at all** for the next, which is the partially-informed pass F4 could not express. *Verify: V2; V59.*
  - **Done, rewritten whole against schema 3, and the granularity is demonstrated rather than merely permitted.** `catalog-api` records `postgres` and `kafka` and carries **no record at all for `events`** — the partially-informed pass F4 could not express, with the note saying so in the manifest itself. `migrates` names `postgres` alone, so the startup migration no longer makes W = yes against `events` and `kafka`: F5's amplification, stopped, in the shipped example. `search-indexer` carries a `postgres` record for a store its `dependsOn` never names, which is TD-1's *undeclared store still classified*. The example measures **8 of 9 pairs recorded**, asserted by a row that fails at 0 and at 9 alike — an example recording every pair would document the old all-or-nothing world in new field names. The `acceptedRisks` entry was re-reasoned onto the undetermined pair, which is exactly when a bypass is needed.
- [x] 1b.4 In `references/discovery.md`: the classification pass. Every runnable unit × **every** `backingStores` entry, not merely the stores a unit declares. Omit rather than guess, and state that an omitted record MUST NOT be written as, or later read as, checked-and-none. A degraded pass — resolver unavailable, static parse standing in — writes the degraded confidence so the gate declines to count it without discovery having to lie about what it did. No record is inferred from another pair of the same unit. Record each store's provider eligibility (local / managed / remote / host-native / undetermined) derived from the discovered address and lifecycle, **never from the store's name, image, or engine**, with the reason recorded so it survives into the refusal message. *Verify: V59; V65; V67; V61.*
  - **Done.** The classification pass is every runnable unit × **every** `backingStores` entry, with `dependsOn` able to add and never subtract. Omission is stated as the mechanism for partial knowledge and an **omitted record** is forbidden from being written as, or later read as, checked-and-none. A `false` is stated as a claim that costs evidence like any other. The degraded pass writes `confidence: "inferred"` rather than omitting, so the manifest stays valid and the cache survives while the read side declines to count it. No record is derived from another pair of the same unit. Provider eligibility is recorded per store as `locality`, **derived from the discovered address and lifecycle and never from the store's name, image, or engine**, with the reason recorded so it survives into a later refusal message; undetermined is read as remote and refuses.
- [x] 1b.5 In `references/shared-state.md`: determinacy is read at the granularity it was recorded — a record about one store is evidence about that store alone, and one store's absence does not make its siblings undetermined. Name the defect in the spec's own terms: `writes: ["postgres"]` was a *positive* claim that simultaneously asserted checked-and-none for every other store, so a partially-informed pass had to omit the field entirely and make all four undetermined; **the missing capability was granularity, not the ability to write a negative**. *Verify: V59; V61; V3.*
  - **Done.** W and X are read from the pair's own record; N is unchanged. Three sentences carry the rule and each has a check keyed on the sentence rather than on the file: determinacy is read at the granularity it was recorded, the defect is named in the spec's own terms — `writes: ["postgres"]` was a *positive* claim that simultaneously asserted checked-and-none for every other store, and **the missing capability was granularity, not the ability to write a negative** — and **one store's absence does not make its siblings undetermined**. Added beyond the task text and worth naming: **a record contradicting the `competesOn` entry beside it is undetermined, not negative**, so the weaker of two readings never wins; `competesOn` holds the identity knob and never the boolean. The `serviceFingerprint` rule is restated **per record**, so re-examining one pair renews nothing for a pair nobody looked at again.
- [x] 1b.6 Report the pair set's derivation, in `references/discovery.md` and in the run's output: the runnable units selected, the `backingStores` entries, and the resulting pair count — 39 × 4 = 156, not 43 × 4 = 172, because a `runnable: false` entry contributes no pairs. Two runs at the same commit with the same manifest state report the same three counts. *Verify: V67.*
  - **Done, and computed rather than asserted.** `references/discovery.md` states the derivation — runnable units selected, `backingStores` entries, resulting pair count — with **39 × 4 = 156, not 43 × 4 = 172**, the reproducibility clause, and the narrowed-count-beside-the-derived-one rule slice 2 will need. `check_schema.py` runs the same derivation over the shipped example: `the pair set derives from 3 runnable units x 3 stores = 9 pairs`, shown reproducible, shown **blind to a `runnable: false` unit**, and shown **able to move** when a runnable unit arrives — the last row is what stops the previous one from passing because the derivation never moves at all.
- [x] 1b.7 Extend `.github/scripts/verify.sh` with V59, V67, V68 and V61's slice-1b half, each with its negative. *Verify: run `sh .github/scripts/verify.sh` on macOS and `alpine/git`.*
  - **Done. macOS: 256 ok, 0 FAIL, exit 0** (baseline at `78b60f9` was 212 ok), from the primary checkout **and** from a linked worktree at `~/Workspaces/stackgraft-worktrees/pfi-1b`, where the C1 row resolved `hash8` through the common dir. **alpine/git: 157 ok, 3 FAIL, and those three are pre-existing** — the same `report under a held lock` / `an empty registry` / `a damaged registry` rows fail at `78b60f9` from a clean clone (141 ok, 3 FAIL), so this slice adds none. The determinacy section is 16 ok / 0 FAIL under alpine; `check_schema.py` skips loudly there for want of `jsonschema`, as it already did. `dash -n` clean on all six scripts.
  - **Every positive carries a negative that must be REJECTED, and the rules were shown load-bearing rather than adjacent.** Four rules were stripped from a scratch copy of the schema and the paired fixtures went red on cue: removing `evidence.minLength` ⇒ `FAIL ACCEPTED but must be rejected: checked-and-none evidenced by an empty string`; removing `serviceFingerprint` from `required` ⇒ `FAIL … a determinacy record with no fingerprint to expire it`; removing `determinacy: false` from the `runnable: false` branch ⇒ `FAIL … a determinacy record on a unit that is never launched`; removing the named `writes` rule ⇒ `FAIL rejected by the wrong rule`. The per-pair **reading** is falsified three ways, because the schema can make a record structurally per-pair and cannot make a reader read it that way: writing `(catalog-api, events)`'s own record moves that pair off undetermined, degrading `confidence` to `inferred` moves it back, and planting a record on another unit moves nothing.
  - **A second measured correction:** the version row first required `"schemaVersion"` and `"const"` on one line and read the shipped schema as declaring **no version at all** once the field gained its description. It now reads across lines, and the count row keys on the declaration site so a second transition cannot hide in the file.
- [x] 1b.8 Re-run the body counter at this slice boundary and assert the same figure 1a.7 recorded — this slice changes no body word, and a check run only at the end cannot say which slice moved it. *Verify: V34.*
  - **Done. The body measures 484 and no body word was touched** — `awk 'f{n+=NF} /^---$/{c++; if(c==2) f=1} END{print n}'` prints 484 before and after, and V34 asserts that figure as a literal on every run, so this slice re-measured it rather than trusting the plan's paragraph. `compatibility` **494 bytes, unchanged**, with its six bytes of headroom untouched. `description` **215 characters, unchanged**. Slices 2, 3 and 4a must re-measure 484 in turn.

## Phase 2: Change-scoped gating (PR 2 — base PR 1b)

- [x] 2.1 **[RED]** Write the narrowing fixtures into `.github/scripts/verify.sh` before the rule exists: a frontend-only diff against a multi-store repository; the same diff against a unit recorded as migrating; a narrowing attempted with no record for that pair; a relieving record whose `serviceFingerprint` has drifted; a unit declaring `dependsOn: []`; and a copy of `references/shared-state.md` with the `dependsOn` prohibition sentence deleted. Record that all fail for want of the rule. *Verify: V58; V60 — all RED at this task.*
  - **Done, RED demonstrated with real output against the shipped tree before a byte of the rule was written.** The sentence rows land in `verify.sh`'s new `change-scoped gating` section; the six diff fixtures land in `check_schema.py`, the home V2/V3 already use, because a narrowing is a *reading* over (manifest, diff) and a grep cannot run one. RED, verbatim: `FAIL shared-state.md has no '## The subject: the pairs the change can reach' section, so every row below reads an empty file`; `FAIL the narrowing rule does not state: the dependsOn prohibition is restated INSIDE the rule, in those words`; and the same shape for the five passes, the one-directional reason, absence-is-never-relief, the frontend limit, the unscoped migration and the both-counts rule; `FAIL 0 pass(es) claim they may narrow; the rule permits exactly one`; `FAIL traps.md does not name the diff's under-reporting as a trap`. **17 FAILs, exit 1**, against a 256 ok / 0 FAIL baseline at `2f58fe3`.
  - **A false green was found and repaired here, and it is the standard slice-1a/1b set — third slice, third one.** The prohibition's exact words are already in the shipped file: measured before this slice wrote anything, `awk '/dependsOn/ && /narrow/'` matched **2 lines** (7 and 61) and `awk '/less/ && /laziest/'` matched **1** — line 61's *saying less would gate less, and the laziest manifest would be the least refused* is verbatim what T1 demands be restated. So a row keyed on the WORDS reported a prohibition restated inside the narrowing rule over a file that had no narrowing rule at all. **Position is the assertion, so position is what the check reads**: every row extracts the section between its heading and the next `## ` and reads the sentence there. The repair ships with a standing row that reproduces the false green — the prohibition deleted from *inside* the rule while the file keeps line 61, where a whole-file grep still passes and the rule-scoped row rejects.
  - **A second false green, in the negatives rather than the positives, caught by measuring the RED run instead of only the GREEN one.** With no section in the file, stripping a sentence out of nothing left nothing, so **all six deletion negatives printed `ok` beside the six positives that were failing** — a negative satisfied by the section never existing, which is the `overlay-reaping` W4/C3/A10 shape the verification legend names four shipped rows for. Each negative now asserts the **transition**: present before the strip, absent after. Re-run in the same red state: `FAIL the fixture for the dependsOn prohibition deletes a sentence the rule never carried`, six times.
- [x] 2.2 In `references/shared-state.md`: the DS40 pass order, stated as five passes with the narrowing confined to one of them — derive (units × `backingStores`, plus `dependsOn` names, which may only **add**), remove (one pair, one record, **only here**), re-insert (escalation triggers, widening only), classify (W, X, N, one verdict). A pair leaves the subject only on a record for **exactly that pair**, whose fingerprint still matches and whose confidence is `declared`; absent record, drifted fingerprint, `inferred` or `user` confidence, or a record about another store all leave the pair in. *Verify: V58; V60.*
  - **Done.** A new `## The subject: the pairs the change can reach` section, placed before *Two hazards* because the subject has to exist before the verdict table reads it. The five passes are a table and **exactly one row says `only here`**, which is asserted as a count rather than as a presence: two rows saying it would be two places a later reader may put a removal. The relief condition is the spec's, in full — a record for *exactly that pair*, `mutates` and `competes` both `false`, `serviceFingerprint` still matching, `confidence: "declared"` — with absent record, drift, `inferred`/`user`, and a record about another store or another unit each named as leaving the pair in.
  - **The pass order is not decoration and is checked as an order, not as a list.** `check_schema.py` runs the five passes over the shipped example so the rule is falsifiable rather than asserted: 3 runnable units × 3 stores = 9 derived pairs, a frontend-only diff (`apps/storefront/src/App.tsx`) selects `storefront`, its three pairs leave on their own records, and the run gates **0 of 9**. The one row that stops the whole set from being a reader that removes nothing: `the narrowing does relieve: all three of the frontend unit's pairs leave on their own records`.
- [x] 2.3 In the same file and **inside** the narrowing rule, not beside it: restate the `dependsOn` prohibition in those words — saying less would gate less, and the laziest manifest would be the least refused — together with what actually changed, which is only *where* the claim is written. T1 is a spec requirement, not commentary: a reader who takes D2 as the repeal will delete the gate. State the structural reason it is one-directional: a record may only subtract from a set derived independently of it, so the laziest manifest still yields the largest subject and the most refusals. *Verify: V60 — the negative is the file with that sentence deleted, which proves the check reads the sentence rather than confirming the file exists.*
  - **Done, inside the rule and not beside it — which is the whole reason the check is positional (2.1).** The prohibition is restated in those words: *`dependsOn` is a declaration: saying less would gate less, and the laziest manifest would be the least refused*, with what actually changed named beside it — *only **where** the claim is written, never whether a claim is needed* — and T1's consequence stated as the requirement it is: *Read as the repeal, this section deletes the gate.*
  - **The structural reason is stated and then made executable.** *A record may only subtract from a set derived independently of it, so the laziest manifest still yields the largest subject and the most refusals.* `check_schema.py` proves it two ways rather than asserting it: `dependsOn: []` yields a subject **identical** to the shipped `dependsOn`, and a unit declaring all three stores yields the same subject as one declaring none — `the laziest manifest yields the same subject as the fullest one`.
- [x] 2.4 In the same rule, state the limit rather than promise the relief: **a change confined to a unit's frontend does not make that unit stateless.** A unit recorded as migrating keeps W=yes for the stores its entrypoint is pointed at whatever the diff touched; where the pointing is unknown, **every** store of that unit is undetermined and refuses, so an unscoped `migrates` cannot be laundered into relief by a small diff. A pair may be removed in pass 3 and returned in pass 4 within one run, and the run names both events. *Verify: V58.*
  - **Done. The limit is stated and the relief is not promised:** *a change confined to a unit's frontend does not make that unit stateless* — pass 4 runs after pass 3 and outranks it, and overlaying a service that creates its schema at startup still runs that create, whatever the diff touched. Measured against the fixture: with `migrates: {"pointing": "recorded", "stores": ["postgres"]}` on the frontend unit, the same frontend-only diff removes three pairs in pass 3 and returns `storefront::postgres` in pass 4 **with W = yes**, and the run names both events — `re-inserted by migrates with W=yes: storefront::postgres, and the trigger is named` beside `the run names both events: removed in pass 3, returned in pass 4`.
  - **The unscoped case is its own sentence and its own row**, because it is the one an implementer would fold into the first: `{"pointing": "unknown"}` leaves **every** store of that unit undetermined, so the same small diff gates 3 of 3 and all three REFUSE — `rejected: an unscoped migrates keeps every store of that unit, all refusing`. `migrates` is read **before** the record in the reader exactly as the verdict table says, so a checked-and-none `mutates` never cancels it.
- [x] 2.5 Report the counts so a narrowing is visible rather than inferred: the derived count, each removed pair **with the record that removed it**, each re-inserted pair with the trigger that returned it, and the gated count **beside** the derived one, never in place of it. *Verify: V67; V58.*
  - **Done, and V67's second half finally has an executable check.** Slice 1b wrote the sentence into `discovery.md` and computed the derived counts, but nothing narrowed, so the *beside* rule was a sentence with a grep behind it — and that grep is green against the shipped file today, which is why it is not the check. What ships instead is a renderer: the derived count, each removed pair **with the record that removed it**, each re-inserted pair with its trigger, and `gated: 0 pairs, beside the derived 9`.
  - **The negative is a second renderer, not a string edited afterwards.** The first attempt built the negative by `.replace()`-ing the number out of the good report and then asserting it was gone, which tests `str.replace` and nothing else — a check that cannot fail, caught before it shipped. `render(report, overwrite=True)` writes the narrowed count into the derived count's place and the same both-counts predicate must reject it: `rejected: a report that writes the narrowed count into the derived count's place`. A guard row fails the fixture outright if derived ever equals gated, because *beside* is satisfied by one number printed twice.
- [x] 2.6 In `references/traps.md`: a diff under-reports by construction — a store reached only at runtime, a connection string read from the environment, an entrypoint that migrates — so the diff is now safety-load-bearing and absence of a record is undetermined, which refuses. *Verify: V61; file review that the trap is checkable against a manifest or a real run.*
  - **Done.** One bullet under *Shared state*, ahead of the migration trap it generalises: the diff now selects the gate's subject, so it is **safety-load-bearing rather than a convenience**, and it under-reports by construction — a store reached only at runtime, a connection string read from the environment, an entrypoint that migrates. Each is named as *a pair the diff would have relieved for free*, which is why relief needs a record and why absence of a record is undetermined, which refuses. **Checkable against a real run rather than only readable**: the trap ends by naming what to compare it against — the derived count, every removed pair with its record, and the gated count beside the derived one, with *a run that prints one count has narrowed something it cannot show you*.
- [x] 2.7 Extend `.github/scripts/verify.sh` with V58 and V60 and their negatives; re-run the body counter and assert 1a.7's figure unchanged. *Verify: V58; V60; V34.*
  - **Done. macOS: 299 ok, 0 FAIL, exit 0** (baseline at `2f58fe3` was 256 ok / 0 FAIL), identical from the primary checkout **and** from a linked worktree at `~/Workspaces/stackgraft-worktrees/pfi-2`, removed after. **alpine/git: 179 ok, 3 FAIL, and those three are pre-existing** — `report under a held lock`, `an empty registry`, `a damaged registry` fail identically at `2f58fe3` from a clean clone (157 ok / 3 FAIL), so this slice adds none. All **22** rows of the new section are green under alpine: they are POSIX `awk` and `grep` over the shipped prose, no `jq`, `node`, `python3`, `flock`, `timeout`, `lsof` or `ss`. `check_schema.py` skips loudly there for want of `jsonschema`, as it already did in 1b. `dash -n` clean on all six scripts.
  - **The body counter re-measured at this boundary rather than trusted: 484 before and after**, and V34 asserts it as a literal on every run. `compatibility` **494 bytes, unchanged** — its six bytes of headroom untouched. `description` **215 characters, unchanged**. Slices 3 and 4a must re-measure 484 in turn.
  - **Every positive carries a negative that must be REJECTED, and the rules were shown load-bearing rather than adjacent.** The one-directional property is an invariant over **8** manifests, not a sentence: *a pair may only be removed if it would have classified REUSE anyway*. Each evidence clause of the relief reader was then dropped in turn and the invariant caught it, with the pair and its verdict named — `dropping the declared clause … (storefront::kafka was removed while classifying REFUSE)`, `dropping the fingerprint clause … (storefront::postgres was removed while classifying REFUSE)`, `dropping the reaches clause … (catalog-api::kafka was removed while classifying ISOLATE)`, `dropping the this pair clause … (storefront::events was removed while classifying REFUSE)`. Cross-checked from outside the file too: deleting a clause from the shipped `relieves` in a scratch copy sends the invariant row red — `FAIL the narrowing removed a pair that does not classify REUSE: ['a drifted record on a selected unit']`.
  - **A third false green, found by that same scratch-copy check and repaired.** With the first five cases, deleting the fingerprint clause outright left the headline invariant **green**: every record in the shipped example is fresh, so the clause was never reached and the row was certifying a reader it never exercised — a case list that cannot present a clause with the input that clause is for. Three manifests (degraded, drifted, record-never-written) were added to `CASES`, and all three clause deletions now fail the invariant directly.
  - **Changed lines 605** (605 additions, 0 deletions) against a ~280 forecast — over the forecast and over the 400 guideline, reported rather than absorbed, the same way 1b's 575 was. Where it went: **394** in `check_schema.py`, which is the whole executable narrowing reader plus 8 invariant cases, 4 clause falsifiers, 5 pairs-that-must-stay, 2 `dependsOn` rows and 2 count rows; **186** in `verify.sh`, which is 22 rows of which 13 are negatives, since the file's rule costs one fixture per assertion and this slice adds nine; **24** in `shared-state.md` and **1** in `traps.md` — the shipped prose is 25 of the 605. One compression pass is in the number (five multi-line docstrings folded, −17). The remainder is not compressible without deleting an assertion. **The reason it is not ~280 is structural and worth naming for slice 3, forecast at ~380:** the three properties this requirement makes non-negotiable — one-directional, cannot relieve a unit's own launch, evidence-bound — are properties of a *reading* rather than of a document, and a reading cannot be falsified by a grep. Slices 3 and 4a should expect the same.

## Phase 3: Coordination identity (PR 3 — base PR 2)

- [x] 3.1 **[RED]** Write the identity and name-family fixtures before the file exists: an X-only pair with the runtime's volume and instance inventory diffed; a W-and-X pair whose diff **must** show a provision, so the row is shown able to see one; each of the three proof legs failing in turn (value equals the base stack's, base value unreadable, no delivery route); a shared-queue pair; a substrate absent from the table; two branches whose slugs truncate to the same 28 characters; a label form scanned for `_` and measured for length. *Verify: V56; V57; V64 — all RED at this task.*
  - **Done, RED demonstrated with real output before `coordination-identity.md` existed: 38 FAILs against a 299 ok / 0 FAIL baseline at `2eb357f`.** Verbatim: `references/coordination-identity.md is absent, so every row below reads an empty file`; `coordination-identity.md states 0 of the three proof legs`; `the knob table carries 0 row(s)`; `the SQL form has drifted from 1.1.0's: | feat/pfi-coordination-identity:  != sg_feat_pfi_coordination_identi_6a655886` (five branch shapes); `the label form is not a legal label: … nothing generated`; `3 line(s) describe an allocated namespace name: … shared-state.md:107: … Redis \`SELECT n\``; `14 line(s) still use the retired {{isolationName}}`; `the closed placeholder set reads '{{isolationName}} {{port}} {{repoRoot}} {{store}} {{templateName}} {{worktree}}'`; `shared-state.md carries no backticked pointer to references/coordination-identity.md`.
  - **The slice-3 false green, found by measuring first and hunting deliberately — fourth slice, fourth of the same family.** Before this slice wrote anything, `awk '/base stack/ && /configuration/ && /differ/'`, `awk '/environment/ && /overlay/ && /route/'` and `awk '/substrate/ && /ends the competition/'` **each matched `shared-state.md`**, because step 2 of the verdict table is where the identity procedure lived at `1.1.0`. A row keyed on the words anywhere in the tree therefore reported a three-part proof over a tree with **no `coordination-identity.md` in it at all.** The repair is the move this slice actually makes: the rows assert a **position and a home** — three legs in the file that owns the rule, **zero** in the file that used to, exactly one home across `references/`. The standing negative is the old text put back, in `1.1.0`'s words rather than this change's: `shared-state.md still restates 3 leg(s) of the identity proof, so the tree carries two texts with two outcomes`.
  - **Six of the new negatives were themselves false greens, printing `ok` while the file was absent, and every one is repaired to assert the transition.** Measured in the first RED run: *exactly one shipped file carries the identity proof* (green because `shared-state.md` was the only home — a count of one satisfied by the rule sitting in the wrong file); the restatement fixture (appending a restatement to a file that already carried all three legs rejects nothing); *no knob row answers how to create a namespace* and *every knob row answers Yes or No* (an empty table has neither); *a derivation stating no truncation* (a generator that produces nothing anyway); *a seventh placeholder added* (the shipped set already differed from the recorded one); and the GNU-only row (a grep over a file that does not exist). Each now checks the shipped file is on the right side of the line **before** its fixture crosses it.
  - **A seventh, in the loop machinery rather than in a fixture, caught while merging the sentence rows: `printf … | while read` runs the loop in a subshell, so every `fail` increments a copy of the counter that dies with it.** The rows would print `FAIL` and the suite would still exit 0. Fed by a redirect instead, and proved: with one sentence broken the suite prints the two expected FAILs and **exits 1**.
- [x] 3.2 Create `skills/stackgraft/references/coordination-identity.md`: the knob rows moved out of `shared-state.md` — `group.id`, a durable name, a queue name, a subject prefix, an advisory-lock key, a replication-slot name — answering **only** *what is this substrate's identity knob* and never *how do you create a namespace here*. The three-part proof (distinct, delivered, substrate-confirmed) with any one unproven leaving X **undetermined**, so the loop terminates on evidence rather than repetition. A store-level environment map MUST NOT serve as the channel — it belongs to the store and is shared by every service paired with it. The standing statement that an overlay with its own consumer group receives every message the base stack receives, so any externally visible side effect is now duplicated. *Verify: V56; V57; V64; V61; V42.*
  - **Done, 70 lines.** Six knob rows, one per coordination primitive, each naming the recordable token as its first backticked cell — `group.id`, `durable`, `subject-prefix`, `queue`, `advisory-lock`, `replication-slot` — and every one asserted present by name rather than by row count. **The table's third column is what makes leg (c) executable**: three rows answer `**Yes**` and three `**No**`, and `check_schema.py` parses the table rather than restating it, so the substrate leg comes out of the shipped file. Proved by flipping it: with `group.id` answered `**No**` the same proven pair stops clearing.
  - **CI-1 is unreachable rather than discouraged, which is what the requirement asks.** `resolve()` returns the mechanisms and the runtime objects each one creates: an X-only pair yields `['identity']` and **zero** provisions, a W-and-X pair yields `['identity', 'copy']` and two — the row is shown able to see a provision rather than passing because nothing ever provisions — and a W-and-X pair with the identity unrecorded **refuses and provisions nothing**, which is `the copy alone leaves X undetermined` as a run rather than a sentence.
  - **All three legs fail on their own and for their own named reason**, which is the check the verification legend says four shipped rows got wrong: `the recorded value equals the value the base stack attaches under`; `the base stack's own value could not be read, so nothing was compared`; `no route sets that variable in the overlay's environment`; `the launch completed without applying the recorded variable`; `the substrate table covers no knob named 'scheduler-slot'`; `no distinct value ends the competition on 'queue'`. Beside them a row the six would be worthless without: **the proof does clear a pair**, or every refusal above is a reader that refuses everything.
  - **The knob table may not answer the other question, and the negative is not hypothetical**: the pattern is the literal cells of the column this slice deletes from `shared-state.md`, so the fixture is that column being *moved* into the new table instead of retired. Measured: `the knob table names a namespace-creation procedure: | Kafka consumer group | \`group.id\` — New database (\`TEMPLATE\`) or schema + \`search_path\` | …`.
- [x] 3.3 In `references/shared-state.md`: delete **the In-instance isolation column of the per-substrate table, not the table.** Four of the six cases with no safe answer rest on the *catch* column, so a reader who deletes the table deletes the evidence for the refuse-cases — T1's hazard wearing different clothes. **Carried open item (decision-state 4a): `specs/shared-state-safety/spec.md`'s Verify clause says "the substrate namespace table is gone" and must be narrowed to the column before this task lands**, or the tree carries two texts with two outcomes, which is what `overlay-reaping` task 3.4 had to unwind. Link to the identity procedure rather than restating it. *Verify: V57; V64; file review that the six no-safe-answer cases are identical in number and in wording to `1.1.0`.*
  - **Done, and the carried-open spec sentence was narrowed first** (decision-state item 4a, commit `3ebe911`), so the shipped file and the spec never disagreed for a commit.
  - **The column is gone and the table is not, and both directions have a negative.** 14 substrate rows survive with their catch column; the table is asserted **two columns wide**, which closes the hole the first check left — a third column renamed on its way back in. Measured against a fixture with the column restored: `the per-substrate table still carries the In-instance isolation column` **and** `the per-substrate table is 3 columns wide, so a third column came back under another name`. Against a fixture with the heading removed: `rejected: shared-state.md with the per-substrate table removed whole`.
  - **A false RED found here and repaired, which is the intent-blind grep pointing the other way.** The file is obliged to say what it deleted — *its In-instance isolation column is gone* — and a whole-section grep read that sentence as the column still being there. The row now counts **table rows** carrying the string, because a column is a table column and not a mention.
  - **The six cases with no safe answer are frozen as literals taken from `v1.1.0`**, having first been diffed against it and found identical, so the check compares against `1.1.0`'s wording and not against today's. Count and wording both asserted; the negative removes one case and the count row reports 5.
  - Step 2 of the verdict table now **links** rather than restates: it names `references/coordination-identity.md` as the only source of what makes an identity count, states that any part unproven leaves X undetermined, and sends the pairs that file refuses to step 5. `proof_legs` measures **0** legs in `shared-state.md` and **3** in the new file.
- [x] 3.4 In `assets/manifest.schema.json`: retire `{{isolationName}}` and add the two family members as distinct placeholders with defined sources — SQL identifier `sg_<slug>_<hash8>` and DNS/object-store label `sg-<slug>-<hash8>`. The set stays **closed**: any other `{{…}}` invalidates the template, no placeholder yields an allocated value, and no field describes a bounded pool, an exhaustion case, or a release obligation. Land this in the same slice as the reference file that names it, because `check_schema.py` cross-checks `references/*.md` against the schema and widening its `FOREIGN` list is how that check stops being able to fail. *Verify: V2; V56; V57; V61.*
  - **Done.** `{{isolationName}}` is retired everywhere it was a placeholder a template writes — 14 shipped lines, including six in the example — and it survives only where the file is naming what it replaced, held to the same `RETIRED`-style exclusion the retired `writes` array is. The closed set is asserted as an **equality** rather than a floor: `{{isolationIdent}} {{isolationLabel}} {{port}} {{repoRoot}} {{store}} {{worktree}}`, because a seventh member is exactly how a placeholder yielding an allocated value gets in. Negative: a fixture adding `{{isolationIndex}}` drawn from the sixteen slots is rejected.
  - **`FOREIGN` was not widened, which is the rule the design gives for this task.** Every backticked camelCase token the new file introduces landed in the schema in this slice: `nameForm`, `baseIdentity`, `deliveryRoute`. The cross-check reads 34 fields and `coordination-identity.md` names 7 of them, and V61's slice-3 fixture proves the cross-check can fail **on that file specifically** — the shipped self-test only proves the two sets differ, which a references directory that stopped being globbed would also satisfy.
  - **Addition beyond the task text, recorded rather than absorbed, and it is the `locality` situation from 1b repeating.** Task 3.6 obliges discovery to record the base stack's value and the delivery route, and DS39 makes the projection a per-store recorded field; `additionalProperties: false` means an unlisted field cannot be written, so all three would have shipped unimplementable. `competesOn[]` gains `baseIdentity` (`{value, source}`) and `deliveryRoute` (`{value, reason}` over a four-way enum whose `none` is the honest refusing answer), and `backingStores[]` gains `nameForm` (`{value, reason}`). **The `allOf` now requires all three beside `overlayIdentity`**, which is what makes *recording an identity is not a re-classification* structural: the material the proof reads cannot be omitted from the record that claims it. Five paired negatives, each rejected by its own named rule; `nameForm` stays **optional** on purpose, and the accepting fixture proves a store recording none is still expressible — required, it would force a guess for every store that never needs a generated name.
- [x] 3.5 State the derivation once, in `coordination-identity.md`, and make the separator the **projection's** rather than the slug's: lowercase the branch, split on every run of characters outside `[a-z0-9]`, drop empty segments, **carry no separator in the slug**, truncate the joined length to 28, `x` if nothing survives; each consumer joins the same segments with its own prefix and separator. That makes *no underscore in a label* true by construction rather than by review — post-processing `_`→`-` is where F1 came from. `hash8` stays over the **untruncated** branch name. *Verify: V56 with both negatives.*
  - **Done, and the derivation is EXECUTED rather than reviewed, which is where most of this slice's size went.** `verify.sh` generates both forms by taking the prefix, the separator, the truncation and the hashing command **out of the shipped table** — the C1 pattern applied to a second recipe — and compares the SQL form against `1.1.0`'s transform frozen beside it as a deliberately different algorithm (one substitution over the whole string, rather than a segment list). Byte-identical on five branch shapes: `feat/pfi-coordination-identity`, `feature/checkout-rewrite-phase-two`, `Release/2.0.0`, `___` and `x`.
  - **Four negatives, all measured, and three of them prove the generator reads the file rather than carrying its own copy of the rule.** Change the label row's Join cell to `_` ⇒ `the label form is not a legal label: | sg-feat_pfi_coordination_identi_6a655886 carries an underscore` (five branches). Change the SQL row's Prefix cell to `sg-` ⇒ `the SQL form has drifted from 1.1.0's: sg-feat_pfi… != sg_feat_pfi…`. Delete the `**28**` ⇒ the generator **stands down** rather than assuming it: `nothing generated`, on every row, which is C1's lesson — a checker that supplies the missing step cannot see it go missing. Fourth: a variant taking `hash8` over the **truncated** slug is shown to collide on the twin branches, so the row asserting two branches differ in **both** forms is exercised rather than adjacent.
  - **F1 measured against a real substrate, which is the regression case this member of the family exists for.** MinIO in a throwaway container, both forms generated from the shipped derivation for `feat/pfi-coordination-identity`: `mc mb m/sg_feat_pfi_coordination_identi_6a655886` ⇒ `mc: <ERROR> Unable to make bucket … Bucket name contains invalid characters` — every bucket `1.1.0` could name, rejected. `mc mb m/sg-feat-pfi-coordination-identi-6a655886` ⇒ `Bucket created successfully`. Container and network removed after.
  - **The post-processing prohibition is a stated rule with its own deletion fixture**, because on these inputs `tr '_' '-'` over the SQL form produces the same bytes as the label projection — the defect is the *rule*, not the output, so the check is that the file states the separator belongs to the projection and that no form is produced from another.
- [x] 3.6 In `references/discovery.md`: for every `competesOn` entry record the knob, **the value the base stack attaches under** read from its own configuration rather than assumed, **the environment variable the service itself takes that identity from** read from the service's configuration and never invented or defaulted from a sibling, and whether the launch method has a **route** that sets it in the overlay's environment — which for a container-run overlay is not the launching shell's. No route, or an unreadable base value, refuses **before** anything launches. *Verify: V64; V61.*
  - **Done, as four numbered things rather than a sentence**, because three of them are what the proof reads and a pass that records the value alone has recorded nothing that can re-classify anything. The base value is read from the base stack's own configuration with `source` recording where; **an unreadable value writes no value**, which is the honest state and refuses, rather than a value written from expectation being compared against nothing. The variable is the service's own, **never invented, never defaulted from a sibling, never derived from a convention**. The route is recorded per entry, and a launch with none records `none`.
  - `backingStores` gains `nameForm` in the same pass, read from the substrate's own grammar or its own rejection and **never from the store's engine name** — the same discipline `locality` is held to, one field along.
  - **Kept deliberately under the one-home rule**: `discovery.md` states what to *record* and never restates the proof that reads it. Measured — `proof_legs` counts 2 of 3 legs there and 3 only in `coordination-identity.md`, so the home row stays exact.
- [x] 3.7 Delete the integer allocator wherever it is named — no allocation, no slot pool, no exhaustion case, no release obligation, in any shipped file. An index-addressed store gets a seeded copy like every other store; sixteen host-global slots that nothing owns are a pool two worktrees draw the same value from and never detect. *Verify: V57; V42 — the portability grep is intent-blind, so the retired vocabulary must not survive in a comment or in a negative fixture's own prose.*
  - **Done. Three shipped lines named it and all three are gone**: the per-substrate table's Redis keyspace cell went with the column, and the rung-3 mechanism list and the rung-3 lifecycle bullet no longer name it. The example's Redis note is rewritten to say why it is never done, and the prohibition ships in `coordination-identity.md` with its reason.
  - **The grep's exclusion is ONE word — `never` — and that is measured rather than chosen for style.** A looser exclusion is a bypass: `no ` excuses the shipped rung-3 bullet, which carries *no create command, no drop command* on the same line it used to name the allocator, so the rule would have gone on shipping under a grep reporting it gone. Every shipped line that names the vocabulary carries `never`; the negative — `| Redis keyspace | \`SELECT n\` — 16 logical databases, allocated per worktree and released on teardown |` — carries none and is caught. Proved load-bearing by deleting the word from the shipped prohibition: `1 line(s) describe an allocated namespace name: coordination-identity.md:66: **Sixteen host-global logical-database indexes … has no integer member …**`.
  - **The pattern is also narrower than "any word about allocation", and that is measured too.** `docs/HOW-IT-WORKS.md` says *allocate a candidate port* about the port picker — a legitimate allocation of something that is not a namespace name — and `Logical database selection does not isolate channels` is one of the six cases whose wording this slice may not touch. Both would have gone red under the obvious pattern.
  - **Deviation from the task text, stated rather than smoothed over.** The task says an index-addressed store *gets a seeded copy*; the copy is slice 4a's to build, and writing it into a shipped file now would claim a mechanism that does not exist — the thing 1a.11's Status rule exists to prevent. What ships is the half that is true at this commit and stays true at 4a: **no integer member, no index selected, and such a store records `mechanism: "none"` and is answered by whatever answers every other writing pair.** CI-4's *Index-addressed store gets a copy* scenario closes at 4a, not here.
  - V42's slice-3 half uses a **wider** pattern on a **narrower** subject, and the reason is measured: `flock`, `timeout`, `lsof` and `node` all ship today for legitimate reasons — `with-lock.sh` says *no flock*, `traps.md` tells the agent to read `lsof` on the host — so widening the tree-wide grep would fail rows about text that is already correct. `coordination-identity.md` carries none of them and is checked against the wider list, with a fixture naming one **inside a comment** to prove the grep is intent-blind.
- [x] 3.8 Extend `.github/scripts/verify.sh` with V56, V57, V64, V42's slice-3 half and V61's; re-run the body counter and assert 1a.7's figure unchanged. *Verify: V34.*
  - **Done. macOS: 379 ok, 0 FAIL, exit 0** (baseline at `2eb357f` was 299 ok / 0 FAIL), identical from the primary checkout **and** from a linked worktree at `~/Workspaces/stackgraft-worktrees/pfi-3`, removed after. **alpine/git: 237 ok, 3 FAIL, and those three are pre-existing** — `report under a held lock`, `an empty registry`, `a damaged registry` fail identically at `2eb357f` (179 ok / 3 FAIL), so this slice adds none. All **58** rows of the new section are green under alpine: POSIX `awk` and `grep` over the shipped prose, no `jq`, `node`, `python3`, `flock`, `timeout`, `lsof` or `ss`. `check_schema.py` skips loudly there for want of `jsonschema`, as it did in 1b and 2. `dash -n` clean on all five scripts.
  - **The three counters re-measured at this boundary rather than trusted: body 484, `compatibility` 494 bytes, `description` 215 characters — before and after, and identical from the worktree.** `SKILL.md` is untouched by this slice: it appears in no diffstat against the tracker, so the body is sealed rather than merely unchanged in count. V34 asserts 484 as a literal on every run. Slice 4a must re-measure 484 in turn.
  - **V61's slice-3 half extends the link loop to the references directory**, which is the only thing that can prove the new file's pointers resolve: V40 forbids the body to name it, so `SKILL.md`'s own loop can never reach it. 20 backticked skill paths across `references/`, all resolving, with `shared-state.md`'s new backlink asserted by name and a broken-link fixture as the negative.

## Phase 4a: Provider and copies (PR 4a — base PR 3)

- [x] 4a.1 **[BLOCKING — answer before writing the script; this is DS41's second named blocking risk.]** Does the shipped `--format` string surface a volume's byte size as a plain string on the Docker versions users actually run? DS36 refuses when the size cannot be measured, so a missing field turns **every** copy into a refusal rather than a degraded report. Fail-safe, but the remedy differs by answer. Record the answer here with the exact command that produced it, on at least two Docker versions. *Verify: recorded observation with its command — no shipped row can cover a question about the daemon's own output surface.*
  - **Answered, on two daemons, and the answer is: it surfaces a size but not a plain byte count.** Second daemon obtained by running `docker:24-dind` privileged, so both are real daemons rather than one client twice. Server **29.5.3** and server **24.0.9**, same 7 MB fixture volume, same three commands:

    | source | 29.5.3 | 24.0.9 |
    |---|---|---|
    | `docker system df -v --format '{{json .Volumes}}'` | `"Size":"7.34MB"` | `"Size":"7.34MB"` |
    | `docker volume inspect --format '{{json .UsageData}}' <vol>` | `null` | `null` |
    | `docker run --rm -v <vol>:/d:ro alpine du -sb /d` | `7344130` | `7344130` |

  - **So the remedy is DS43's third row and no parser is needed.** The field exists on both versions, so the fail-safe never fires spuriously — but it is a human string with a unit, and a POSIX `sh` that had to read it would need suffix and decimal handling for a value the runtime does not promise to keep stable. **One correction to DS43, measured rather than inherited: on this fixture the string was STABLE across three consecutive calls** (`7.34MB` each time), where DS43 recorded the same volume reporting `611.2MB` and `611MB` minutes apart. Both observations stand; the instability DS43 saw is real and is a rounding boundary a small volume does not reach. Neither makes the string parseable.
  - **Two findings beyond the question, both of which would have shipped as defects.** (a) **`du -sb` is supported by busybox** (`-b`, *apparent size (including holes)*, busybox 1.37) as well as by GNU coreutils, so no image this change controls has to be pulled — but `--entrypoint sh` is mandatory, because an image declaring its own entrypoint is otherwise handed the measurement as arguments to the engine, and the run fails in a way that reads exactly like an unmeasurable volume. Measured: `alpine/git` declares one, and without the override the size row refused every store. (b) **`du -sb` and `du -sk` measure different things and the gap is unbounded** — on a sparse fixture, `214748368902` against `8`. The provider uses the apparent size, which over-states a sparse source and is the conservative direction.
  - **A third finding, which is a defect in DS36's stated command.** DS36 says *one `df -P /` inside a throwaway container*. Measured on this host, `df -P` reports **512-byte blocks** and the same flag inside a minimal image reports **1024-byte** ones: the host filesystem answered `330306512` under `df -P` and `165152016` under `df -Pk`. Reading each side in its own unit **reports the host as twice as free as it is**, which is the permissive direction and would have shipped as a silent doubling of the only figure that binds. The provider uses `df -Pk` on both sides and `verify.sh` fails the shipped script if a bare `df -P` reappears anywhere in it.
- [x] 4a.2 **[RED]** Write the provider's refusal and cleanup fixtures into `.github/scripts/verify.sh` before the script exists: `destroy` with a worktree argument that does not equal the object's label; an unlabelled volume; a volume whose `stackgraft.labels` value this run does not recognise; a provision against a filled filesystem, with the runtime's **volume list compared before and after**; a space probe that cannot answer for either filesystem; and the host filesystem diffed across a full provision-address-destroy cycle. Record that all fail for want of the script. *Verify: V43; V44; V45; V46 — all RED at this task.*
  - **Done, RED demonstrated with real output against a tree with no provider and no reference file: 37 FAILs, exit 1, against the 379 ok / 0 FAIL baseline at `384cb55`.** Verbatim: `scripts/provider-docker.sh is absent, so every provider row below reads nothing`; `references/isolation-providers.md is absent, so every provider row below reads an empty file`; `1 of the five shipped scripts are missing from skills/stackgraft/scripts`; `the contract names 0 of the three operations`; `the provider is absent, so the two-filesystem row reads nothing`; `the provider is absent, so the unfiltered-removal row reads nothing`; `the provider is absent, so its usage rows ran nothing`; `isolation-providers.md never states that the free-space check is a candidate rather than a guarantee`; `isolation-providers.md states no blind spot, so its numbers read as complete`; `4 of the four refusal cases are unnamed in isolation-providers.md`; `4 of the copy's four labels are unnamed in isolation-providers.md`; `shared-state.md carries no backticked pointer to references/isolation-providers.md`.
  - **Two of those 37 were FALSE REDs on pre-existing shipped prose, and finding them is the point of measuring the red run rather than only the green one — fifth slice, fifth of the family.** (a) The guarantee grep, written as a bare `guaranteed`, fired on `references/discovery.md:40` — *substituting this checkout is the one answer **guaranteed** to be wrong* — which is correct text about something else entirely. (b) The duration grep, written with a bare `worst case`, fired on `references/reaping.md:166` — *20 seconds **worst case**, never an unbounded wait* — which is a **stated bound of a deterministic algorithm**, the opposite of a prediction and the kind of statement this repository wants more of.
  - **Both were narrowed against the measurement, and the duration one twice.** Every guarantee alternative now carries its space context. The duration pattern with copy context alone **still** fired, on `references/discovery.md:120` — *the thing worth **cloning usually** lives in the main worktree* — an adverb about where something lives. It now also requires a figure in seconds, minutes or hours. **All three shipped lines are frozen as standing rows that must NOT match**, because a pattern that turns correct prose red is a pattern that gets loosened rather than obeyed. Verified on `/usr/bin/grep` and on busybox `grep` inside alpine: zero matches tree-wide, all four prediction shapes caught, all three shipped lines clean.
  - **The negatives that could only stand down were repaired to assert the transition**, the slice-2 repair applied ahead of time: `the blind-spot fixture is empty, so stripping the sentence proves nothing` and `the one-sample fixture stripped the figures too, so it exercises the wrong condition` both fired in the RED run, which is what a deletion fixture must do when there is nothing to delete.
- [x] 4a.3 Create `skills/stackgraft/scripts/provider-docker.sh` per DS35: three operations, tab-separated plain-string stdout (`volume` / `instance` / `bytes` / `seconds` / `host` / `port` / `env` / `refused`), exits `0` ok / `2` usage / `3` refused with nothing created or removed / `4` environment failure. The copy is taken by a second container mounting the source `:ro` and the destination `rw` from the store's own image, so the base engine is never signalled, stopped, or reconfigured. **The actuator re-verifies, the caller only names**: `destroy` re-finds its target with `--filter label=stackgraft.repo=<hash8>` plus the complete label set and removes only objects whose `stackgraft.worktree` **equals** the argument, normalised on both sides per DS27; instance first, volume second; a volume that will not remove is an exit 4 naming it, the `with-lock.sh` unremovable-aside precedent. Never `volume prune`, `system prune`, `compose down --volumes`, or any unfiltered removal. Parses no JSON, writes no host file, touches neither manifest nor sidecar. *Verify: V5; V41; V42; V43; V44; V45.*
  - **Done, 516 lines, and exercised end to end for real rather than only parsed.** `dash -n` clean; V5's shipped glob picked it up the moment it existed; the named inventory is now five with its own deletion negative. It parses no JSON — every runtime value is asked for one at a time with `--format` — writes no host file under any verb, and the host filesystem is enumerated before and after a full cycle and found byte-identical.
  - **`provision -> address -> destroy` was run against a store engine no shipped file mentions**, which is IP-1's *unenumerated store engine* scenario executed rather than asserted: `alpine/git` as the base store, a 3 MB seeded volume, the copy read back as carrying `seeded-by-the-base-stack`, the runtime inventory identical at the end of the cycle.
  - **Three deviations from DS35, each recorded rather than absorbed, and the first is the load-bearing one.**
    **(a) `provision` takes one more argument: `<base-instance>`.** DS35's line is `<source-volume> <image> <label>...` and the mount destination was to be resolved here. Resolving it needs `docker ps --filter volume=<src>`, which carries no ownership filter — and **this repository already fails a shipped script for exactly that**: `verify.sh`'s `ps_listings` row reported `unfiltered-2` against the first draft. The caller already holds the value, because `sourceVolume`, `image` and the instance all come out of ONE inspection discovery has already done, so taking it as a parameter asks for nothing new. What is still **not** asked for is where the engine keeps its data: that is read from the instance, because a caller supplying it would be the engine table under the caller's name. The schema records it as `baseInstance` beside the other two.
    **(b) One record kind added: `space`, in four shapes** (`measured`, `bound`, `copies`, `blindspot`, `candidate`). DS35's eight kinds predate DS36's reporting obligation, which has no home in them; folding a filesystem name into `bytes` would make one kind mean two things.
    **(c) The base container's environment, command and single-element entrypoint are carried onto the copy.** DS35 says *run the same image on it*, and taken literally that produces a container which exits immediately: an image started under a command the compose file supplied is not the same instance when started without it. All three are read back like every other runtime fact. **A multi-element entrypoint override is refused with the shape named**, because the runtime's own CLI takes one string and a copy quietly started under a different entrypoint from the one holding the base stack's data is worse than a refusal.
  - **`destroy`'s exit semantics follow `reap.sh`'s rather than DS35's sentence.** DS35 reads *3 refused, nothing created or removed*, which is exactly right for `provision` and wrong for `destroy`: a destroy that removes one object and refuses another has not left everything alone. Exit 3 there means **at least one object was refused**, every object that proved out was still removed, and each refusal is named — which is `reap.sh`'s rule and is stated in the header.
  - **A defect found by measuring and fixed: the label-set version may not be in the QUERY.** With `stackgraft.labels` in the filter, an object carrying a version this run does not recognise is never found, so it is never reported, so a destroy over it exits 0 saying nothing — while DS35 requires *reported, never removed*. The query now scopes to repository and store, and the version is re-verified in the actuator like every other property of the target. Proved load-bearing: restoring the filter sends `the future-labelled volume: exit 0, still present yes, said ''` red.
  - **A second defect, and it is the more expensive one: `docker rm -f` leaks an anonymous volume per copy.** A store image commonly declares a volume path of its own, and any such path this run does not mount over becomes an unnamed volume the run created — carrying no label, findable by no scoped query, leaking one per provision for ever. Measured on the fixture image, which declares `/git`. `destroy` now removes the instance with `-v`, which takes the anonymous volumes and leaves every named one alone. Proved load-bearing: reverting to `docker rm -f` gives `1 of 1 anonymous volume(s) this run created survived the destroy, findable by nothing`.
- [x] 4a.4 Free space per DS36, in the script and in `references/isolation-providers.md`: **two** filesystems — the runtime's data root by one `df -P /` inside a throwaway container, and the host filesystem holding the cache directory, which is the binding one because the runtime's disk is a sparse image growing into the host. Either unmeasurable ⇒ refuse. The projected size is the source volume's **measured** size, never an estimate; unmeasurable ⇒ refuse. Name which filesystem bound the decision, report this repository's copies already on it from the scoped label query the report pass already makes, and **print the blind spot**: another worktree's copy that does not exist yet, another repository's copies, and every other consumer of the disk are not counted and cannot be. **Carried open item (decision-state 4b): `specs/isolation-providers/spec.md`'s "refused before it starts rather than failed during it" overstates what any check can do across concurrent worktrees and must be reworded to a candidate, not a guarantee, before this task lands.** A copy that runs out of space mid-write removes its own partial, exits 3, and refuses the pair. *Verify: V45; V46; V47.*
  - **Done, and the carried-open spec sentence was reworded first** (decision-state item 4b, commit `c107f89`), so the shipped file and the spec never disagreed for a commit. IP-6 is now *Free space is measured on the filesystem the copy will occupy, and reported as a candidate*, obliging the blind spot, the binding filesystem and the mid-copy cleanup, and forbidding *enough space*, *guaranteed*, *reserved* and *will fit*. Two scenarios added: a mid-copy exhaustion that leaves the object inventory as it found it, and a report held to the candidate wording.
  - **DS36's stated command is corrected, and the correction is a safety fix rather than a style one.** `df -P` is 512-byte blocks on the host and 1024-byte blocks inside a minimal image, so the shipped sentence would have reported the host as twice as free as it is — the permissive direction, on the only figure that binds. Both sides use `df -Pk`. `verify.sh` fails the script if a bare `df -P` reappears, and reverting one call site sends `the provider measures with a bare df -P somewhere` red.
  - **The refusal is deterministic and costs nothing to test.** The fixture is a sparse file — 200 GB of apparent size on 8 KB of disk — so the arithmetic really refuses: `the copy needs 214748368902 bytes and /dev/disk3s5 has 164957760KiB, so it is refused before any bytes are written`, exit 3, with the runtime's volume inventory **identical** before and after. The mid-way case is exercised too, and with a real failure rather than an injected one: a container already holding the instance's name is exactly what a crashed earlier run leaves behind, and the provision removes its own partial volume and exits 3 with the inventory identical.
  - **A measured correction to the harness, not to the requirement: the inventory snapshot must be SCOPED.** Taken over the whole runtime, it read every object on the developer's machine, so any unrelated container a live base stack started between two snapshots read as debris — and the cycle-closes row failed for exactly that on a host with running stacks. It is scoped to this skill's own name prefix, which still covers every name the provider derives, and the section asserts the host holds no such object before it starts.
- [x] 4a.5 Create `skills/stackgraft/references/isolation-providers.md` **in this slice, not 4b** — `shared-state.md`'s slice-4a edit backlinks to it and the link loop resolves every backticked `references/…` path, so a dangling link fails 4a's own CI. Content: the three operations, the Docker implementation, **the second runtime on paper** (Kubernetes and host-native, each with a named answer for all three operations, both marked unbuilt), copy ownership and lifetime, free space, the refusal cases. No engine name appears in the contract section. *Verify: V61; V42; file review — grep the contract section for engine names and find none.*
  - **Done, 131 lines.** The contract, the Docker implementation, the second runtime on paper with all three operations answered for Kubernetes and for host-native and **both marked unbuilt**, free space, the live-copy residual, the four refusals, ownership and removal, and what the run reports. It joins the reference link loop automatically and `shared-state.md` carries the backticked pointer to it.
  - **The no-engine rule is checked over the SECTION rather than the file**, because position is the assertion: the contract may name none and the evidence paragraph must name several. Nineteen engine names are greped for and the fixture is the contract section with one row added — `| provision | copy the postgres data directory |` — which is caught.
  - **The no-fourth-operation row reads the TABLE, not the prose, and that is a repair.** A grep for the word fired on the file's own sentence saying *there is no `verify` operation* — an intent-blind pattern pointing the wrong way, the slice-3 false RED in another guise. The row now extracts the operation set the contract table defines and requires it to be exactly `address destroy provision`; the negative is the table gaining a fourth row.
  - **V42's slice-4a half holds both new artefacts to the wider floor** — the provider script and the reference file are checked against `$PORTABILITY_NEW` and `$GNUISM`, with a provider-shaped fixture naming an unavailable tool **inside a comment** as the negative. Three real hits were found and reworded in the script's own header, which is what an intent-blind grep is for.
  - **V61's slice-4a half:** `check_schema.py` cross-checks this file's backticked field names against the schema on **that file specifically**, because a references directory that stopped being globbed would leave the shipped self-test green; the fixture is the file's own text plus one camelCase token the schema does not define, and it is rejected.
- [x] 4a.6 In `references/shared-state.md`: ISOLATE means a seeded copy by default — it carries the data the base stack has and asks the repository for nothing — while in-instance isolation stays as the zero-disk optimisation with **every** rule intact: the command is discovered and never embedded, no client binary is assumed present, the placeholder set stays closed, the deny-list applies to the template's grammar, the substituted command is executed as an argument vector with no shell fallback, a program that re-parses its argument is rejected, the destructive-verb class is judged by effect, and both halves are approved together. Half a lifecycle records `mechanism: "none"`, which selects the **copy**, not a refusal. State that an empty namespace is a different thing from a copy. *Verify: V61; V3; file review of the two-hazards mapping — W buys a copy, X buys a name, and neither is recorded as satisfying the other.*
  - **Done, and the verdict table is where the change is real rather than described.** Step 4 is now **ISOLATE in place** and step 5 is **ISOLATE by a seeded copy** where the store records a usable provider, refusing only where it does not. A new section ahead of *Isolating in place* opens with the two-hazards mapping in one sentence — **W buys a copy, X buys a name** — and states that neither is ever recorded as satisfying the other: a distinct identity protects not one byte of data, and a copy ends no competition on any substrate.
  - **Every in-instance rule is restated as intact rather than left to be inferred**, in one sentence naming all eight, with the reason the road is still preferred: it copies nothing. Not one of them is relaxed by the existence of the other road.
  - **The half-lifecycle bullet was edited where it lives** rather than contradicted from the new section: *the record is `mechanism: "none"`, which leaves this road … It is no longer the end of the pair: step 5 answers it with a seeded copy.* The rung-4 row in `docs/SHARED-STATE.md` reads the same way.
  - **The empty-namespace distinction is stated as what a feature is tested against**, which is the operative form of it: a freshly created database has the shape of the base stack's state and none of its contents, so a change that reads what the base stack loaded cannot be exercised in it. That is why the empty namespace is the optimisation and the copy is the default.
- [x] 4a.7 In `assets/manifest.schema.json`: `backingStores[].isolation` admits a **provider reference** beside the in-instance record, both expressible in one entry and a store with neither expressible too. Lands in the same slice as `isolation-providers.md`, per the same `check_schema.py` rule as 3.4. *Verify: V2; V3; V61.*
  - **Done, and all three shapes are proved by fixtures rather than permitted by omission.** `isolation.provider` is an object requiring `runtime`, `sourceVolume`, `image`, `baseInstance` and `confidence`, with `additionalProperties: false`. `check_schema.py` carries **three acceptances and three rejections**, because a property that is merely optional is satisfied by a schema that never validates it at all: both records in one entry **accepted**; a provider with no in-instance mechanism **accepted**; neither **accepted**; a provider with no `sourceVolume` — a copy with nothing to copy — **rejected**; a `runtime` this contract does not define **rejected**; and an engine-shaped extra field **rejected**.
  - **`runtime` has three members and deliberately no `managed` or `remote`.** A store whose state is not on this host has no local volume to name, so it records no provider at all and refuses through `locality` — which is slice 1b's field, **read here rather than duplicated**, exactly as that slice's note anticipated. `kubernetes` and `host-native` are legal and refuse by name, so a manifest can say what is true about a repository this version cannot serve.
  - **The shipped example demonstrates the granularity rather than only permitting it**, which is what 1b's rewrite established: `postgres` carries both records, `events` carries a provider with `mechanism: "none"` — the common shape this version exists for — and `kafka` carries the in-instance record and no provider. The example says so in its own notes.
  - **Addition beyond the task text, recorded rather than absorbed, and it is the third time this has happened in this change:** `provider.baseInstance`. Task 4a.3's argument list needed it and `additionalProperties: false` means an unlisted field cannot be written, so without it the argument would have had no source and the instruction would have shipped unimplementable. It is the same inspection that produced `sourceVolume` and `image`, so it asks discovery for nothing new.
- [x] 4a.8 In `references/reaping.md`: store copies as reap targets — candidacy stays a **positive closed allowlist** (this repository's complete label set with its `hash8`), the same liveness rule an overlay is judged by, and **removal requires the removal flag in addition to the mutation flag**, because a copy is the more expensive thing to have destroyed by accident, not the less. An unlabelled volume, or one whose `stackgraft.labels` this run does not recognise, is reported and never acted on. A runtime that cannot be queried reports **unknown**, never zero copies. *Verify: V44; V66.*
  - **Done, as a new section 9a placed between the allowlist it inherits and the liveness rule it reuses**, so both are read before the difference is stated. Candidacy is the same positive closed allowlist; liveness is decided the same way against the worktree list.
  - **The one difference is stated with its reason, and the reason runs opposite to intuition on purpose:** removal takes the removal flag **in addition to** the mutation flag because an overlay container stopped by accident is restarted from its image in seconds, while a copy removed by accident is state nothing on this host can reproduce — the base stack has moved on since it was taken. The instance running on a copy stays an ordinary container target; the copy itself is what the second flag protects.
  - **The label set for a copy is four, not the overlay's five, and the reason is `reap.sh`'s own header:** `stackgraft.service` holds a manifest service key, and writing one onto a volume would make it claim to be a launched service. A partial set is reachable by no query, which is proved at runtime rather than asserted — a volume carrying repository and store labels and nothing else answers the complete-label query **zero** times, beside a row proving that same query does find this run's own copy.
  - **Unknown-never-zero is carried over with the highest cost attached**, in section 14's own terms: a report saying *no copies* over a daemon that never answered tells a developer their disk is clear when it may hold gigabytes, and they will believe it because it is a number.
- [x] 4a.9 In `SECURITY.md` (T2): the copy as a data surface — a duplicate of whatever the developer's base stack holds, on the same disk, under a name this skill chose. The answer is ownership, labelling, a named lifetime and an output contract that says the copy exists and how to remove it, stated as the posture rather than as encryption. *Verify: V66; file review.*
  - **Done, as its own section between *What is enforced* and *What is not covered*.** The surface is named in the file's own register — where the base stack holds a production-shaped dump, so does the copy — and the posture is stated as **ownership, labelling, a named lifetime and an output contract, not encryption**, with the reason: encrypting a copy the developer's own engine has to read moves the key onto the same disk and changes nothing.
  - **Five mechanisms, each matching what the code does:** the complete four-label set with *complete or the object is not ours*; the lifetime being the worktree, judged against git's worktree list and never a timer; every run naming the copy and its exact removal command **including a run that removed nothing**; removal taking the removal flag on top of the mutation flag; and the crash-consistency residual stated rather than described as small.
  - **The limits are in the same plain register the file uses elsewhere**, and they say what is not there: the copy inherits the confidentiality of the disk it sits on and nothing more, it is not encrypted, not redacted and not sampled, and a developer whose base stack holds data they may not duplicate should not point this at it.
  - **Observation carried out of scope rather than fixed here, the same way task 1a.2 recorded a stale entry it left alone:** `SECURITY.md` line 21 still describes *a closed placeholder set of five* naming `{{isolationName}}`, which slice 3 retired. Slice 3's grep was scoped to `skills/stackgraft`, and this file is outside it. Correcting it belongs to that slice's scope, and fixing it silently from here is the two-texts-in-the-tree hazard introduced by the wrong slice.
- [x] 4a.10 Refusals by name, in the script and in `isolation-providers.md`: managed and remote (no local state to copy, and the skill requests no credential and attempts no in-place isolation), host-native (**unbuilt**, not impossible), and **undeterminable locality treated as remote** — an unknown is not a permission. Discovery's recorded reason is carried into the message, and the refusal **does not cascade**: the unit's other pairs are still classified on their own. *Verify: V65.*
  - **Done, as a table of four in `isolation-providers.md`** — managed, remote, host-native, locality undetermined — each with its reason and what the run does, plus the sentence that discovery's recorded `locality` reason travels into the message so the refusal says what was observed rather than restating the rule. *An unknown is not a permission* is stated in those words; host-native is **unbuilt**; the managed row states that no credential is requested and nothing is isolated in place there.
  - **The script's own half is what it can observe rather than a restatement of the gate's:** a store whose runtime object is not on this host has no local state to copy, and it refuses there too.
  - **The non-cascade is RUN, not asserted.** Two provisions in one fixture: `remotestore` against an absent volume refuses at exit 3 naming the store and `no local state to copy`, and `fixturestore` beside it **still provisions at exit 0**. A row that only read the sentence would pass over a blanket stop.
- [x] 4a.11 T5, across every shipped file: report the store, the bytes copied and the elapsed time **measured on this run**, and predict nothing — not from a byte count, not from another store's rate, not from a previous run. The 244 MB/s against 72 MB/s spread is evidence that rate tracks file profile rather than size, so a figure derived from a size is derived from the wrong variable; a cited measurement is labelled as one sample. *Verify: V54.*
  - **Done, and the reporting half is read out of a real run rather than out of the prose.** The provision's own output is parsed for `bytes` and `seconds`, both required to be integers and the byte count required to be non-zero — a copy reporting zero bytes is not a copy.
  - **The prediction ban is a tree-wide grep over `skills/stackgraft`, `README.md`, `docs` and `SECURITY.md`**, and it catches all four shapes of a prediction: typical, takes, expected, and worst case. It was narrowed twice against measured false REDs on correct shipped prose (task 4a.2), and **both shipped lines are frozen as standing rows that must not match**.
  - **The one-sample rule is a CONDITIONAL row rather than a presence row:** cite a figure and you owe the label. `isolation-providers.md` cites the measured spread — 244 MB/s against 72 MB/s, and 647 MB in 9 s beside 10.0 GB in 42 s — labelled as one sample, with *that is one sample, not a rate to plan against* beside it. The negative strips the label and must fail; a guard row fails the fixture outright if it strips the figures too, which is the case where the deletion proves nothing.
- [x] 4a.12 Q2, stated in one place rather than two: the copy is taken **live** and is therefore **crash-consistent**, and an engine with an fsync-ordering dependency may not recover from it. The residual MUST NOT be described as covered, mitigated, handled, or unlikely, and no shipped file may assert that a live copy is safe for every engine. Verification is what catches the cases where crash consistency was not enough, which is why it is mandatory rather than advisory. *Verify: V52; V54 — the negative is a fixture line using each of the four forbidden words.*
  - **Done, in one section, and the row checks the two statements sit TOGETHER rather than merely both existing** — the live-copy claim and the fsync residual are required in the same `## ` section, which is what *in one place rather than two* means when a check has to read it.
  - **Five fixtures, not four, and all five must be caught:** covered, mitigated, handled, unlikely, and *safe for every engine*. Measured together — `the residual grep caught only N of the five excuses` is the failure message, and the shipped tree matches none of them.
  - **V52 is run against a real base store and the row is shown able to see a disturbance.** The base container's status and restart count are unchanged across a copy; the negative **stops a base store on purpose** and requires the same predicate to report it, comparing status and pid before and after — so the row is not passing over a copy that disturbed nothing because nothing happened. The pid is the strongest of the three: a restarted engine gets a new one, and `StartedAt` alone would read a recreate as continuity.
- [x] 4a.13 Extend `.github/scripts/verify.sh` with V41–V47, V52, V54, V65, V66 and their negatives; re-run the body counter and assert 1a.7's figure unchanged — this slice changes no body word. *Verify: run `sh .github/scripts/verify.sh` on macOS and `alpine/git`; V34.*
  - **Done. macOS: 479 ok, 0 FAIL, exit 0** (baseline at `384cb55` was 379 ok / 0 FAIL), identical from the primary checkout **and** from a linked worktree at `~/Workspaces/stackgraft-worktrees/pfi-4a`, removed after. **alpine/git: 296 ok, 3 FAIL, and those three are pre-existing** — `report under a held lock`, `an empty registry`, `a damaged registry` fail identically at `384cb55` from a clean clone (237 ok / 3 FAIL), so this slice adds none. The provider section contributes 55 green rows under alpine; the runtime rows skip loudly there for want of a daemon, as the shipped container rows already do.
  - **The three counters re-measured at this boundary rather than trusted: body 484, `compatibility` 494 bytes, `description` 215 characters — before and after, and identical from the worktree.** `SKILL.md` appears in no diffstat against the tracker, so the body is **sealed** rather than merely unchanged in count. V34 asserts 484 as a literal on every run.
  - **A defect the SECOND platform found and the first could not.** Under alpine the row `the provider rejects an unknown verb` failed: with no container runtime present, an unknown verb exited **4** and told the caller the runtime was missing rather than that the verb was wrong. The verb is now decided at the top of the script and the dispatch at the bottom carries no unknown branch. The row asserts **exit 2** rather than any non-zero code, which is what told the two failures apart — a row taking any non-zero code would have reported the environment failure as a rejected verb on every machine without docker.
  - **Every shipped rule was proved load-bearing by breaking it, with real output.** Three shipped rules were reverted in one run and exactly three distinct rows went red, each for its own reason: `the provider measures with a bare df -P somewhere, which is 512-byte blocks on one side and 1024 on the other`; `the future-labelled volume: exit 0, still present yes, said ''`; `1 of 1 anonymous volume(s) this run created survived the destroy, findable by nothing`.
  - **A defect in the harness itself, caught by checking the fixture rather than the row.** The part-way-failure fixture plants a container holding the instance's name. On a run where the provision had failed, `$pname` was **empty** — and `--name ""` does not fail, it lets the runtime pick a name, so the fixture created a stray container nothing here would ever remove and the row passed for the wrong reason. It now stands down loudly when the provision named no instance. The stray it left was found by diffing `docker ps -a` against the pre-work snapshot, and removed.
  - **The suite's own leak was measured, and the result is a finding rather than a fix.** A full run leaks **18 anonymous volumes**, and that number is identical at `384cb55` before any of this slice's code exists — it comes from fixture containers in the instrumentation and reap sections started without `--rm` and removed without `-v`. This slice's own two were fixed, so the run leaks 18 rather than 20 and **this section leaks zero**, asserted by its own row. The pre-existing 18 belong to earlier slices and are recorded for the chain close rather than repaired from here.
  - **One compression pass is in the number, and it ADDED coverage.** Nine near-identical grep/ok/fail blocks became one `doc_states` helper and eight one-line rows; the helper then earned its own falsifier, so the eight can no longer all pass over a file nobody read. Net **+14** lines and one more assertion — the same shape slice 3's second pass took.
  - **Changed lines 1796** (1777 additions, 19 deletions) against a ~380 forecast and against the ~800 slice 3 said to plan for. **The forecast is wrong for the fifth time, and by 4.7× against the original and 2.2× against the revised.** Where it went: **923** in `verify.sh` (100 new rows, of which 46 are negatives, plus one full provision-address-destroy harness with its own cleanup); **516** in `provider-docker.sh`; **131** in `isolation-providers.md`; **67** in `check_schema.py`; **44** in the schema; **23** in the reworded spec; and **64** across `shared-state.md`, `reaping.md`, `SECURITY.md`, `README.md`, `docs/SHARED-STATE.md` and the example. **The shipped prose is 195 of the 1796.**
  - **The reason it is not ~800 is one step past slice 3's, and it is worth stating for slices 4b and 5.** Slice 3's checks were readings and a derivation, so it shipped a generator. This slice's are readings, a derivation **and a mutation of the host**: proving that `destroy` removes only what it made, that a refused provision leaves no partial, and that the base store was not disturbed all require **starting and removing real runtime objects and diffing the inventory around them** — which no grep and no reader can do. That harness is ~330 of the 923, and it costs a fixture base store, a seeded source volume, an inventory snapshot function, and a cleanup that is itself asserted. **Slice 4b is forecast at ~300 and inherits this harness rather than rebuilding it, but it adds a discriminator probe that starts a second empty instance per candidate — plan against ~600, not ~300.**
- [x] 4a.14 Update `README.md` and `docs/SHARED-STATE.md` Status: isolation now means a copy, the copy is **not yet verified**, and slice 4a alone must not be released. State it plainly — an unverified copy the overlay writes into is the false green this change exists to prevent, and 4a is exactly that state until 4b lands. *Verify: file review — no claim that a copy counts as isolated.*
  - **Done, and it is the FIRST bullet under *Honest limits* rather than a qualification at the end**, because a reader who stops after one line must have read this one: *a writing pair now isolates onto a copy of the store's state, and that copy is **NOT YET VERIFIED**. This step alone must not be released.* It says what ships, says what is missing, and names the consequence in the change's own terms — an unverified copy the overlay then writes into is precisely the false green this whole change exists to prevent.
  - **`docs/SHARED-STATE.md` carries the same statement before anything else in its new section**, so the mechanism is introduced already qualified rather than qualified afterwards: *read the rest of this section as a mechanism that exists rather than one you can rely on.*
  - **No file claims a copy counts as isolated.** The README's second new bullet describes the copy as a data surface with its labels, its lifetime, its removal command and its two flags, and describes the space check as **a candidate and not a guarantee** — which is the wording the shipped grep enforces everywhere.
## Phase 4b: Verification and lifetime (PR 4b — base PR 4a)

- [x] 4b.1 **[BLOCKING RISK, already answered by DS42's measurement — record it rather than re-run it.]** DS41's first blocking question was *how many of the 43-service repository's four stores declare an exec-form healthcheck*. DS42 measured it: **zero of four** supply a rung-1 discriminating candidate — postgres `CMD-SHELL`, timescaledb `CMD-SHELL`, redis exec-form but `redis-cli ping` answers identically on an empty instance, minio exec-form but a health endpoint, the one shape IP-2 names by name. Record that table in `references/isolation-providers.md` and the consequence in `README.md`: **at slice 4b, against that repository, 2.0 provisions and then refuses for want of a rung-2 read, and slice 5's generated read is the remedy.** Per decision-state item 1 this slice's acceptance therefore uses a fixture repository carrying a discriminating exec-form healthcheck; the gap is declared here, not discovered at verification. *Verify: file review of both statements; V49.*
  - **Done, recorded rather than re-derived.** DS42's four-row table ships verbatim in `references/isolation-providers.md` — postgres and timescaledb `CMD-SHELL` (excluded by the argv rule before anything else is asked), redis exec-form but `redis-cli ping` answers `PONG` on an instance holding nothing, minio exec-form but a health endpoint — under the heading **zero of its four stores supply a rung-1 candidate**. Six rows assert it: four naming the stores individually, so a missing one is *named* rather than counted, plus the consequence sentence and its deletion negative.
  - **The consequence is stated in `README.md` as a shipped, temporary state**, in the *Honest limits* bullet slice 4a opened for the unverified copy: *against a repository like that one, **this version provisions a copy and then refuses every writing pair***, with the reason (rung 2's only source there is a generated read) and the fact that generating one is the next step rather than this one. That is the `overlay-reaping` *nothing reaps yet* posture, applied where it was forecast.
  - **The two shipped negatives were confirmed on real engines rather than cited**, because a sentence about `pg_isready` is cheap to write and cheap to be wrong about. On `postgres:16` and `redis:7-alpine`, an empty instance beside a seeded one: `pg_isready -U postgres` → `/var/run/postgresql:5432 - accepting connections`, exit 0, **identical**; `redis-cli ping` → `PONG`, **identical**; `psql -tAc 'SELECT 1'` → `1`, **identical**, which is DS42's generated read failing its own discriminator; and a schema-agnostic count → `0` against `1` on postgres and `0` against `2` on redis, which is the shape that clears. Those five measured strings are the case values in the readback rows, so the check carries the measurement rather than a paraphrase of it.
- [x] 4b.2 **[RED]** Write the discriminator fixtures into `.github/scripts/verify.sh` before the mechanism exists: a candidate that **does** differ against an empty instance (the fixture repository's healthcheck); `pg_isready` and `redis-cli ping` as the two shipped negatives, both measured to answer identically on an empty instance and both of which must be **rejected as a query**; DS42's generated `SELECT 1`, which must be rejected exactly as `pg_isready` is; and a candidate admitted with no probe having run at all. Record that all fail for want of the readback. *Verify: V49; V50 — all RED at this task.*
  - **Done, RED demonstrated with real output against the shipped tree before a byte of it changed: 30 FAILs, exit 1, against a 479 ok / 0 FAIL baseline at `6f3cdd6`.** Verbatim: `isolation-providers.md has no '## Verifying the copy' section, so every verification row below reads an empty string`; `the verification section never states: a start is not proof`; `5 of the five stand-ins for the query are unnamed in the verification section`; `the verification section never states: the copy must match the base store byte for byte`; `the verification section never states: the candidate is a query only once it discriminates against an empty instance`; `the rung table names 0 of its three sources`; `the measured rung-1 table does not name postgres` (and timescaledb, redis, minio); `the lifetime section never states: a copy is made once per (worktree, store) and reused`; `the shipped table defines 0 distinct exec route(s), so the three outputs are not comparable`; `the shipped table carries no start line for the empty instance, so the probe rows below read nothing`.
  - **The rows that were GREEN in the RED run are recorded rather than glossed, because that is the C1 hazard in this slice's own harness.** The reader rows — `harvest`, `readback` and their clause falsifiers — pass over a tree with no mechanism in it, since the reader IS in `verify.sh`. That is why two things are extracted from the shipped files instead: the re-parsing program list out of `references/shared-state.md`, and the exec route and the empty instance's start line out of the shipped table. Both were RED, and both are what stops a checker from supplying the step whose absence is the defect. **The boundary that remains is stated rather than papered over:** the readback's two comparisons are asserted positionally in the shipped file and implemented in `verify.sh`, and nothing proves those are the same two sentences. What is proved is that the file states them, that deleting either fails a row, and that dropping either clause of the reader flips a case.
- [x] 4b.3 Implement the discriminator probe per DS34: **one empty instance of the same image, no volume, started and destroyed**, permitted by the provider carve-out because it creates a labelled runtime object and removes it. Recorded once per `(store, command)`, expiring on that store's fingerprint, re-run only on drift — paid once, and it costs seconds because an empty store image is not a 10 GB volume. **The command qualifies as a query only once the probe has shown its output differs on an empty instance**; matching alone proves the copy carries the base's state, discrimination proves the command could have said otherwise. *Verify: V49 with all three negatives.*
  - **Done, and the probe is the AGENT's rather than a fourth provider operation** — which is what DS34 says twice and IP-1 requires. `provider-docker.sh` gains no verb: the contract table still defines exactly `address destroy provision` and the shipped row asserting that is untouched. The probe is one `docker run -d --rm` of the same image with no volume, started and stopped by the run that needs it, and DS34's *permitted by the provider carve-out* is satisfied as the carve-out reads it — a labelled runtime object, created and removed.
  - **Three properties of that start line, and each is the difference between a probe and a leak**, all three read out of the shipped table and asserted: it mounts nothing, so it can hold no state; it is started with `--rm`, so the **runtime** removes it and its anonymous volumes and **no command here ever names an object to remove** — there is nothing to mis-target; and it carries this repository's hash and worktree so a run that died still leaves something a person can find. Measured negatives: dropping `--rm` fails the row (`1 of the empty instance's four start properties are missing from the shipped line`), and mounting state into it is caught by the same reader.
  - **The probe deliberately carries no `stackgraft.store`, and that has its own row.** The complete four-label set is what a copy *is*; a probe answering the copy's own scoped query would be a copy nothing ever seeded, and `destroy` would then be able to reach it. Measured: adding `stackgraft.store=$store` to the shipped line gives `the empty instance carries stackgraft.store, so the copy's own scoped query would return it`.
  - **A candidate is a query only once the probe has run, and that is a clause of the reader rather than a sentence.** `readback` refuses on `probed != yes` before it compares anything; dropping that clause admits an unprobed candidate and fails the row. The result is recorded per `(store, command)` as `verification` in the schema, expiring on `sourceFingerprint` — added in this slice because `additionalProperties: false` means an unlisted field cannot be written, so the instruction would otherwise have shipped unimplementable. That is the fourth slice in a row this has happened; it is recorded rather than absorbed.
- [x] 4b.4 Implement the readback: the candidate is run **against the base store and against the copy** and the outputs must match byte for byte. **The query is issued by the agent, not by the provider** — `address` returns the instance handle and the agent execs through the same route `applyVia: compose-exec` documents; a fourth provider operation would let the provider certify its own output, the property DS29 spent a whole rationale removing from `reap.sh`. On mismatch, on a query that cannot be issued, and on a query that cannot be derived at all: **destroy the copy and refuse the pair**, and never wire the overlay to the base store instead. *Verify: V51 — the negative inspects the overlay's peer configuration to prove there was no silent fall back.*
  - **Done, and the query is issued by the agent through one route, which is asserted rather than assumed.** The shipped table gives three rows — base store, empty instance, copy — and all three issue the candidate with the same string. `verify.sh` extracts column 4, requires **exactly one** distinct value, and then **runs that string verbatim** three times. The reason is stated in the file and it is the interesting part: *a candidate issued one way against the empty instance and another way against the base store can differ for a reason that has nothing to do with the data — and a difference is exactly the signal being read, so a second route would manufacture the discrimination and admit a liveness ping as a query.* Measured negative: changing the copy's row to `docker exec -i` gives `the shipped table defines 2 distinct exec route(s), so the three outputs are not comparable`.
  - **Run for real, not asserted.** A seeded base store, a provisioned copy and an empty instance of the same image; the shipped route issued three times; a candidate reading what the instance carries clears (`the copy answered the base store's answer, and the query discriminates`) and the empty instance is shown to really answer differently, so the discrimination is measured rather than assumed. A constant-output candidate — `redis-cli ping` and `pg_isready` in the one image this suite may assume — is **rejected as a query** by the same reader.
  - **V51's negative inspects what the overlay would have been wired to.** The copy's bytes are truncated, the readback returns `destroy`, the copy is destroyed, and then three things are asserted together: the copy's instance is gone, `address` for that pair now exits **3** with nothing to hand back, and the output names **no base instance** — so there is no fall back to wire to rather than a rule saying there must not be. `after a failed verification the copy is gone, nothing is addressable, and no address names the base store`.
  - **A design correction found by measurement, stated rather than absorbed: the byte-for-byte match can only ever be a property of the moment the copy was made.** Taken as a per-run check on a reused copy it destroys every copy it is meant to protect — from the first launch the overlay is *writing into the copy*, which is what the copy is for, and the base stack has moved on independently, so the comparison reports the overlay's own work as corruption. That would delete the reuse Q3 exists to make free. So the match runs at provision and on every explicit refresh; **what every later launch still issues is the query against the copy and against an empty instance**, which is re-runnable, costs one empty instance, and is what stops a copy nobody verified — one left behind by a run that died between provisioning and reading back — from being put into service on the strength of existing. Both halves are stated in the shipped file with their own rows and deletion negatives.
- [x] 4b.5 Write the rung table into `references/isolation-providers.md` and the harvesting rule into `references/discovery.md`. Rung 1: the store service's **exec-form** `healthcheck.test` from the resolver output — a `CMD-SHELL` healthcheck is shell source again and falls through, the template contract's argv and no-re-parse rules applying unchanged. Rung 2: a **read** command from the repository's own lifecycle target family, **whose only source on the motivating repository is slice 5's generated read (DS42)** — state that, with 4b.1's measured table beside it. Rung 3: nothing ⇒ destroy the copy, refuse the pair, name the store and say no query could be derived. *Verify: V48; V49; V50.*
  - **Done.** The rung table ships in `isolation-providers.md` with all three sources asserted as a **set** — a file naming rungs 1 and 3 and never the read rung is the chain DS42 found open, and three independent greps would read that as complete. Rung 2 names the generated read as its only source on the motivating repository, beside 4b.1's measured table. Rung 3 says `no query could be derived` in those words, with its own deletion negative.
  - **The harvesting rule is a reader, not a sentence.** `harvest()` takes a healthcheck test vector one element per line and answers `candidate` or `refused` with a reason: an exec-form `CMD` vector is harvested; a `CMD-SHELL` one is refused **naming the shell form** (`a CMD-SHELL healthcheck is shell source again rather than an argument vector`); a `CMD` vector whose program re-parses its argument is refused; a store declaring none is refused. Both clauses are proved load-bearing by dropping them from the reader that actually runs: without the form clause the `CMD-SHELL` vector is admitted, without the deny list the `sh -c` vector is admitted.
  - **The deny list is taken OUT OF `references/shared-state.md`** rather than copied into the checker — the C1 pattern this repository already applies to the `hash8` recipe and the name family. It reads `awk bash dash env eval sh xargs zsh` out of the *may not re-parse* row. **Proved load-bearing by deleting `sh` from the shipped row:** `rejected: an exec-form healthcheck whose program re-parses its argument - the reader answered 'candidate sh -c pg_isready -U postgres'`. A checker carrying its own copy of that list could not have seen it go.
  - **The harvesting rule lands in `references/discovery.md` as `verification`**, one bullet beside `backingStores`: exec form only, never re-written from a shell line, never assembled by the skill, with the vector, its rung, its `sourceFingerprint` and whether it discriminates recorded. A liveness probe records `discriminates.value: false` rather than no record — *checked, and it answers the same on an empty instance* is worth keeping — and a store nobody could harvest a candidate for records nothing and refuses at verification rather than at discovery.
- [x] 4b.6 Copy lifetime per Q3, in `isolation-providers.md` and in the output: **once per `(worktree, store)`**, reused on every later launch, so the first start pays the measured cost and the rest are free. Re-provision only on an **explicit** refresh request; no elapsed time, size, or staleness heuristic may refresh, and none may refuse a launch on age alone. A refresh destroys the old copy and **verifies the new one** exactly as a first provision does. *Verify: V53 with both negatives.*
  - **Done, and the reuse is structural rather than documented.** *Once per `(worktree, store)`*, *only on an explicit refresh*, and *no heuristic refreshes one and none refuses a launch on age alone* each ship as their own sentence with their own deletion negative. A refresh is spelled `destroy` then `provision`, which is what keeps it something a person asked for.
  - **Run for real: two launches reach one identity and the second provision refuses.** `two launches from one worktree reach one instance identity: sg-deadbe02-…-fixturestore`, and a second `provision` over the same triple exits **3** saying `a copy named … already exists; destroy it first if a refresh is what you want` — so nothing is re-provisioned by a launch, and the shipped refusal is the mechanism rather than a rule the caller is asked to obey.
  - **The age-alone prohibition has a grep as well as a sentence.** No shipped file may refresh or refuse on elapsed time, and the fixtures for the four forbidden phrases are caught by the data-age pattern under 4b.7.
- [x] 4b.7 DS38's age wording, as a requirement rather than a style note: state the **absolute timestamp** the copy was taken, the **elapsed time** since, and the standing sentence that this is the age of the **copy** and that what it holds is the base store's state **as of that timestamp** — which says nothing about how far the base store has moved, because the run does not compare. Add the one cheap observable fact: where the base store's **runtime instance identity has changed** since the copy, say so; where it cannot be read, say the comparison was not made. `data is N old`, `up to date as of`, `stale` and `fresh` are comparisons the run did not perform and none may appear. The age is printed on a run that provisions, refreshes and destroys nothing. *Verify: V55; V53.*
  - **Done, and the age is emitted from `address` — which is the whole of why it is unconditional.** `address` is the verb a run that provisions, refreshes and destroys nothing still calls, because the copy has to be addressed before the overlay can be wired to it. Four records: `age taken <absolute timestamp>`, `age elapsed <seconds>`, `age subject <the standing sentence>`, `age base changed|unchanged|unread`. Measured on a run that provisioned nothing: `a run that provisioned nothing still reports the copy's age: taken 2026-08-07T10:51:35Z, 0 second(s) ago`.
  - **The negative asserts the transition rather than the absence.** The same reader over the same output with its `age` records stripped must reject it — `rejected: a run whose output carries no age record - its absence fails the row rather than passing quietly` — and a guard fails the fixture outright if the strip left an `age` record behind.
  - **The one observable fact beside the age is observable rather than inferred, and it is the honest form of DS38's sentence.** The run compares the copy's creation time against the base instance's `StartedAt`: later means the base store's instance has been recreated or restarted since the copy was taken, which is what a restore or a reseed looks like from here. Where the caller named no base instance, or it cannot be inspected, the answer is **`unread`** — *the comparison was not made* — never agreement by default. Both branches are exercised: `the base store's runtime instance identity is unchanged`, and with no base instance named, `a run with no base instance to compare against says the comparison was not made rather than reporting agreement`.
  - **A portability finding, measured rather than inherited: neither obvious way to turn a timestamp into seconds is available on the supported floor.** The flag that parses a date string is GNU-only and its BSD spelling takes a different one, so `iso_epoch()` does the civil-from-days arithmetic in POSIX `awk` — 25 lines, no table, no library, and the UTC offset is **read rather than assumed**, because the runtime answers with its own offset for a volume and with `Z` for a container and taking one for the other moves the age by hours. Round-trip checked: `2026-08-07T05:12:44+02:00` → `1786072364` → `2026-08-07T03:12:44Z`.
  - **V55 is a grep with one measured exclusion.** `up to date as of`, `the data is N old`, a copy that *is stale* and a copy that *is fresh* are all caught; `staleness` and `refresh` are not, and that is why both sides of `stale` and `fresh` carry a word boundary — this repository already uses both words correctly and a pattern that turns correct prose red is a pattern that gets loosened rather than obeyed. The exclusion is **one word, `never`**, the same decision slice 3 made for the allocator vocabulary, and it is proved load-bearing twice: on a fixture, and by deleting `never` from the shipped prohibition, which sends `a shipped file states a data age beside a copy: …isolation-providers.md:149` red. The prohibition itself is a positive row too, so the grep is not satisfied by a tree that says nothing about ageing at all.
- [x] 4b.8 **SKILL.md body — the only body edit after 1a.** Output Contract bullet 5 gains the copy and its age: 15 → 18 words, **+3**, landing at 488 or 487 against 1a.7's measured figure. Nothing else enters the body — not the provider, not the two reference filenames, not the narrowing rule, not a hazard-to-mechanism action cell. *Verify: V34 (the measured figure, asserted as a literal); V35; V39; V40; V1.*
  - **Done, one line, and the measured result is 487.** `The exact teardown command, any isolated namespace left behind, and how to remove it.` → `The exact teardown command, any namespace or copy left behind, its age, and how to remove it.` **484 → 487**, exactly the +3 DS33 forecast, and the plan's *488 or 487* resolves to **487** for the same reason 1a landed at 484 rather than 485 (decision-state item 3). `verify.sh` asserts 487 as a literal with a one-word drift as its negative, and the drift row still reports it as *inside the ceiling and not the recorded figure*, which is what separates that row from the `-le 500` one.
  - **`BODY_ADDS` moved 21 → 3, and that is not housekeeping.** V35 requires `adds-first.md` to be `over_n + <the slice's recorded add count>`; a figure left at a previous slice's is a fixture nobody recalibrated, which is the same drift the hard-coded 38 was carrying. At 487 the derived `over_n` is 14, `over.md` measures exactly **501**, `adds-first.md` measures 504 and is asserted **strictly above** it, `under.md` measures 500 and must be ACCEPTED, and the reintroduced hard-code measures 525 and reports its 25-word margin.
  - **The body is otherwise sealed.** V39 finds no `REUSE`/`ISOLATE`/`REAP`/`COPY` — lowercase *copy* is not a verdict term and the loop is case-sensitive by design — and V40 finds neither reference filename and no hazard-to-mechanism wording; *copy left behind* is not *copy it*, and the shipped negative still catches a gate row that says so. `compatibility` **494 bytes** and `description` **215 characters**, both unchanged and re-measured before and after.
- [x] 4b.9 In `references/traps.md`: a copy that started but was never verified (a start is not proof, and the loss is committed before anything reports it); and a copy's age read as the data's freshness (the two clocks diverge the moment the base store is restored, reseeded, or migrated). *Verify: V55; V61.*
  - **Done, both under *False greens*, which is where `/health` returning 200 already lives** — the neighbour is the point: a start, a socket, a 200 and a zero exit are named there as four ways of finding out nothing.
  - The unverified-copy trap says what makes it silent rather than only that it happens: a crash-consistent copy of a live engine can start perfectly clean and be missing exactly the state the feature under test needs, and by the time anything notices, **the overlay has been writing into it**, so the first symptom is a test result you cannot tell from a real one.
  - The age trap names the three events that part the two clocks — a restored dump, a reseeded fixture, a migration on the base stack — and says the age line still reads ten minutes, *correctly, about the copy*. It ends by naming what to distrust: any output that calls a copy fresh, or calls it stale, is reporting a comparison nobody performed. Worded to sit outside V55's own pattern rather than to be excused by it.
- [x] 4b.10 Extend `.github/scripts/verify.sh` with V48–V51, V53 and V55 and their negatives. *Verify: run `sh .github/scripts/verify.sh` on macOS and `alpine/git`; each new positive demonstrably fails when its negative fixture is substituted.*
  - **Done. macOS: 579 ok, 0 FAIL, exit 0** (baseline at `6f3cdd6` was 479 ok / 0 FAIL), identical from the primary checkout **and** from a linked worktree at `~/Workspaces/stackgraft-worktrees/pfi-4b`, removed after, with the three counters identical there — body 487, `compatibility` 494 bytes, `description` 215 characters. **alpine/git: 371 ok, 3 FAIL, and those three are pre-existing** — `report under a held lock`, `an empty registry`, `a damaged registry` fail identically at `6f3cdd6` from a clean clone (297 ok / 3 FAIL), so this slice adds no fourth. The new section contributes **74 green rows under alpine**; its 17 runtime rows skip loudly there for want of a daemon, as the shipped container rows already do. `dash -n` clean on all five scripts.
  - **91 rows, 40 of them negatives, and every shipped rule was proved load-bearing by breaking it with real output.** Six reversions in two runs, and exactly the expected rows went red, each for its own reason: a second exec route in the table ⇒ `the shipped table defines 2 distinct exec route(s)`; `--rm` dropped from the probe line ⇒ `1 of the empty instance's four start properties are missing`; `stackgraft.store` added to it ⇒ `the empty instance carries stackgraft.store, so the copy's own scoped query would return it`; `never` deleted from the prohibition ⇒ two rows, the statement and the tree-wide grep; `report_age` removed from `address` ⇒ five age rows; `sh` deleted from `shared-state.md`'s re-parse list ⇒ the harvest reader admits the `sh -c` vector.
  - **The loop machinery was checked first, and it is clean.** Slice 3's worst false green was a `printf … | while read` subshell that swallowed every `fail`; this section contains no pipe-fed loop at all — every loop is a plain `for` in the current shell — and the counter is proved live rather than assumed, because both breakage runs exited **1** with their FAILs counted. The one deliberate subshell is the `sec_row` self-probe, which must not add a failure to a working suite, and it is commented as the only place a `fail` is allowed to be lost.
  - **A defect in this slice's own harness, found by checking the fixture rather than the row.** The section's leak envelope was scoped to the two fixture **name** prefixes, and the object most likely to leak — the copy, whose name the provider derives from the worktree hash — begins with neither. A leak row that cannot see the most likely leak is precisely what this file exists to catch. The envelope now asks the scoped label query as well, and a positive control plants an object carrying the label and neither fixture name to prove the envelope reports it (commit `b946c91`).
  - **The suite's leak was measured and this section's share is zero.** A full run leaks **18** anonymous volumes at `6f3cdd6` and **18** with this slice applied — measured across seven runs, 126 volumes, exactly 18 per run — so the section adds none, and its own row asserts it: `this section left no labelled object, no fixture, no probe and no unnamed volume behind`. The pre-existing 18 belong to earlier slices' fixtures and stay recorded for the chain close.
  - **`docker ps -a` and `docker volume ls` are byte-identical to the pre-work snapshot**, diffed rather than eyeballed, with every volume this work created removed afterwards.
  - **One compression pass was considered and declined, with the measurement.** The only fold available is the 24 explicit `sec_row` calls into a data-driven loop, worth about 20 lines of 906 — and it would re-introduce exactly the loop shape slice 3 found swallowing failures, on rows whose patterns contain the delimiter any such loop would need. Declining it is the cheaper trade. The compression that *did* happen is the two helpers themselves: `sec_row` turns 24 positive-plus-transition-negative pairs into two lines each, and `harvest_case`/`readback_case` do the same for ten reader cases.
- [x] 4b.11 Update `README.md` Status: copies are now verified before they count as isolated, **and against a repository whose stores declare no discriminating exec-form healthcheck every writing pair still refuses** until slice 5. State the measured 0-of-4 result rather than describing it as an edge case. *Verify: file review against 4b.1's recorded table.*
  - **Done, and it replaces slice 4a's *NOT YET VERIFIED* bullet in the same first position under *Honest limits*** — the bullet a reader who stops after one line must have read. It says what now holds (three issues of one command, and the copy counts only where it matches the base store byte for byte *and* the empty instance answers something else), what happens when it does not (destroyed, refused, never wired to the base store instead), and then the measured cost **as a number rather than as an edge case**: zero of four stores supply a usable candidate, each of the four named with its reason, and *against a repository like that one, this version provisions a copy and then refuses every writing pair*.
  - **A second bullet carries the lifetime and the age**, in the same register: made once per worktree and store, reused, refreshed only when asked, and *every run reports how old the copy is — the age of the copy, which is not a claim about how far the base store has moved, because the run does not compare them*.
  - **No claim that a copy counts as isolated on the strength of existing**, and no shipped file states a data age beside one.

## Phase 5: Generated lifecycle target and release (PR 5 — base PR 4b)

- [x] 5.1 **[RED]** Write DS37's falsifier fixtures into `.github/scripts/verify.sh` before the generator exists: a target written and never run must **not** satisfy the gate; a teardown executed before any create has been observed must be refused; a file edited after approval must **drop** the approval and re-show the template; a generated drop aimed at a name not derived from the name family must be rejected by the destructive-verb class; and a run that staged, committed, or pushed the generated files must fail. Record that all fail for want of the generator. *Verify: V62; V63 — all RED at this task.*
  - **Done, RED demonstrated with real output against a tree with no generation rule in it: 73 FAILs, exit 1, against the 579 ok / 0 FAIL baseline at `a1fdf12`.** Verbatim: `shared-state.md has no '## ... offers to write one' section, so every generation row below reads an empty string`; `the generated family names 0 of its three files, and a family of two supplies rung 2 with nothing`; `the shipped sentence names '' rather than all four of Makefile, Taskfile.yml, justfile and package.json`; `the shipped table yields '', so the writer below has no name to write`; `0 reference file(s) state the never-append rule`; `the generation rule never states: a generated target is inferred until a run has observed its create, drop and read succeed`; `the writer placed the family in 'no-directory' rather than the repository's existing bin/`; `the byte-identity row cannot see a Makefile that was appended to, so it was never checking one`; `the commit-state row cannot see a staged path, so it was never checking one`; `the generated read did not clear: base '' empty '' copy ''`.
  - **TEN false greens were found in this section's own harness before a byte of shipped prose was written — seventh slice, and the largest set yet, because this slice's rows assert RESTRAINT and a restraint that was never exercised looks exactly like one that held.** Measured by reading the rows that printed `ok` in the red run rather than only the ones that failed. (a) The never-append set was **empty**, so the four-file digest loop digested nothing, the `*absent*` case never matched, and the byte-identity row then compared `''` against `''` and vouched for four files it had never read — **the third `'' = ''` in this file**, after pick-port's stability row and the probe byte-identity row. (b) Four restraint rows — three new paths, nothing else edited, the pre-existing file untouched, nothing staged — all printed `ok` over a repository **nothing had been written into**. (c) The approval fingerprint ran over three **missing** files: `fingerprint.sh` emits `-` for each, three dashes hash to a perfectly well-formed object id (`44e8570f87e98fc492701de42f4627c9bbb08f9a`, measured), and appending to a file that is not there **creates** it, so the edits appeared to move a value nobody had. Only the returns-to-its-approved-value guard caught it. (d) The observation block read three successful lifecycle exits over three files whose whole content was a comment, after which *the drop removed the namespace* passed because the create had never made one. (e) `an absent store key produces no offer and no refusal` was satisfied by a reader that had **stood down** — "not a refusal" is satisfied by silence, and only "is `no-offer`" is not.
  - **Each repair is a premise gate on the MECHANISM rather than on the input**, which is the lesson the reap section's probe rows already carry in this file: the digest loop requires four named files; the restraint rows require three paths to have really landed; the fingerprint block requires every leg to digest to something that is not `-`; the create and the drop each carry a positive control, and the drop row stands down where the create made nothing.
  - **Four more were C1 defects rather than premise defects, and they are the ones that would have survived review**: the reader carried its own copy of a shipped rule, so it could not see that rule go missing. The required count of three, the map a store name comes from, the two names a drop may remove, the directory candidates and the three file names are now all **read out of the shipped file**, and each stands down when it is absent. Proved by breaking the shipped table: with its read row deleted, `rejected: DS37's original pair` reports `the gate answered 'declared runnable' rather than 'inferred runnable'`.
  - **A tenth, in a negative rather than a positive, and it is the `overlay-reaping` W4/C3/A10 shape in this section's own fixture.** `sh -c bin/db-drop-postgres {{isolationIdent}}` is refused by the **name-family** clause — `bin/db-drop-postgres` is an argument that is not a family member — so the row claiming the re-parse rule was enforced was reading a rejection that rule never produced, and the paired empty-deny-list falsifier could not fire at all. The positive asserts the **reason** now, and the falsifier uses `sh -c {{isolationIdent}}`, whose only defect is its program.
  - **Two locale hazards removed with them:** `sort` is locale-collated, so `Makefile` orders before `justfile` under the C locale and after it under `en_US.UTF-8` — an equality against a sorted string would have passed on macOS and failed on the minimal Linux image for a reason with nothing to do with the rule. The sets are checked by membership and count, the shape this file already uses for the five stand-ins and the three rungs.
- [x] 5.2 **DS42: the generated family is three files per store — create, drop, and read — not two.** DS37 as originally written generates a create and a drop, neither of which reads, so DS34's rung 2 has no source and the chain does not close. Write the family as three, with the read as the verification probe. *Verify: V50; V62.*
  - **Done, and three-not-two is structural in two places rather than stated in one.** The shipped table in `references/shared-state.md` defines the family as `db-create-<store>`, `db-drop-<store>` and `db-read-<store>`, and the schema's `generated.files` **requires all three**. A record naming a create and a drop is rejected — `rejected: a family naming a create and a drop and no read at all, which is a chain that does not close` — proved load-bearing by relaxing `files.required` to two in a scratch copy, which sends exactly that fixture red.
  - **The number three is not written into the checker.** `GEN_REQUIRED` is the row count of the shipped table, so the reader follows the file rather than a constant: deleting the read row from the shipped table sends `rejected: DS37's original pair — a create and a drop observed, no read` red with `the gate answered 'declared runnable'`, and a paired negative shows the same reader at a required count of two calling that pair declared. A checker carrying its own `3` could not have seen the table go back to DS37's two.
  - **Rung 2 is pointed at it in the file that owns rung 2.** `references/isolation-providers.md`'s rung-2 row now names a read the run offered to write, and 4b's *this version provisions a copy and then refuses every writing pair* is qualified with *until rung 2 has a source* rather than deleted — 4b's row asserting that sentence is still green, and the tree no longer says generating one is a later step at the moment it is this one.
- [x] 5.3 Write the files as **new executable files under the repository's existing script directory** (`bin/`, `scripts/` — whichever discovery found, never a new convention), named from the discovered `backingStores` key. **Never append to `Makefile`, `Taskfile.yml`, `justfile`, or `package.json`**: appending edits source the skill did not write, in a grammar where a space-indented recipe line is a silent break, and it makes rollback text surgery rather than a file deletion. The skill **never stages, commits, or pushes** — the human's pull request is the review. *Verify: V63; threat-matrix rows *Executable-file classification* and *Commit state*.*
  - **Done, and this is the one rule in the whole change that no grep and no reader can check, so it is checked against a REAL repository.** `verify.sh` builds a throwaway git repository — `bin/existing-thing`, a `Makefile` carrying the developer's own `db-create` recipe, `Taskfile.yml`, `justfile`, `package.json`, one commit — writes the family into it for real, and digests the four untouchable files **before and after**. An appended `Makefile` recipe is invisible to every grep and every reader in this file; only the hash sees it. The repository is created here, mutated here, removed here, and its removal is asserted.
  - **Four defect modes, each a negative that must be REJECTED, each against its own copy of the repository**, measured: a writer that appends its target to the `Makefile` (`the digest moved, which is the only thing that could have seen it`); one that invents `tools/` although `bin/` exists; one that overwrites `bin/existing-thing`, which is the collision the four build files do not cover; and one that runs `git add`, caught by the index being non-empty. The positives assert three new paths under the discovered directory, all executable and all carrying a shebang, `git status --porcelain` showing exactly three `??` entries, nothing staged, and HEAD unmoved.
  - **The directory and the file names come out of the shipped file, never out of the writer.** `bin/` and `scripts/` are read from the placement sentence and the three names from the shipped table, so a rename in the file moves the writer with it; both stand down when absent, which is what made the red run red.
  - **The never-append list is read out of the shipped sentence too**, and it is proved load-bearing: with `package.json` deleted from that sentence the suite reports `the shipped sentence names 'Makefile Taskfile.yml justfile ' (3 token(s)), missing: package.json` and the digest row stands down rather than comparing two empty strings.
- [x] 5.4 The three offer constraints, because a create is harmless and a drop aimed at the wrong database is not: **the store name comes from the discovered `backingStores` key and is never invented**, inferred from a service name, or defaulted — where discovery recorded no key, no target is generated and no offer is made; the content is **shown in full** before anything is written; and it is written only on **explicit approval**. The offer is never a precondition: a decline, a non-approval and an unwritable repository all leave the default seeded copy, and no refusal is issued for the missing target while the default path is available. *Verify: V62; file review of the three constraints stated together.*
  - **Done, the three stated together as a numbered set** in the generation section, each with its own sentence row and deletion negative: the name from the discovered `backingStores` key and **never invented**, the content **shown in full**, and written only on **explicit approval**.
  - **The naming rule is a reader with three answers rather than two, and the third answer is the whole point.** `offer`, `refused` and `no-offer` are distinct: a store whose key discovery never recorded produces **no offer and no refusal**, because refusing there would be a pair refused for want of a target while the seeded copy was available — the rule two sentences up. Measured: a name inferred from the service `catalog-api`, and one defaulted to `postgres` against a repository that runs no such store, are both `refused`; an absent key is `no-offer`; and dropping the discovered-key clause admits the invented name, which is what proves that clause is what refuses one.
  - **A defect found by breaking the shipped rule rather than by reading the row, and fixed in `d65dd6f`.** The row asserted that *some* map was named. With `backingStores` swapped for `competesOn` — a field that exists — the extraction followed it, the reader went on working, and the suite was green. It now fails by name: `the shipped rule takes a generated store name from 'competesOn' rather than from backingStores, which is the map every pair is derived against`. Extraction and assertion are two rules rather than one restated, the shape the closed placeholder set already takes.
- [x] 5.5 Falsifier 1 — a generated target is recorded **`inferred`** until a run has observed its create, its drop **and its read** succeed against the discovered store, recorded as an event with timestamp and exit status; only that observation raises it to `declared`. `declared`-on-write makes the claim and its evidence the same author, which is the shape *a gate on an optional field is no gate* describes. *Verify: V62.*
  - **Done, and `declared`-on-write is unreachable rather than discouraged.** The schema refuses to express it: `confidence: "declared"` beside a `generated` record requires `observed` to carry create, drop **and** read, each at `exit: 0`. Three fixtures rejected, each naming its own defect — nothing observed, a create-and-drop with no read, and three observations one of which is a failure. Stripping that one rule from a scratch schema sends **all three** red on cue and nothing else.
  - **The exit status is recorded whatever it was, and that is deliberate.** A create that ran and failed is an observation too; recording only the successes would make an absent record mean two different things. `exit` is required on every observation — dropping that requirement is measured to send `an observation carrying a timestamp and no exit status` red — and only a zero counts toward `declared`.
  - **The observation is OBTAINED rather than asserted.** Against a real store, the generated create, read and drop are run and their three exit statuses are what the gate reads: `three observed exits against the discovered store raise the generated target from inferred to declared: create 0, drop 0, read 0`. Both mutating halves carry a positive control — the namespace is absent before the create, present after it, and gone after the drop — because an exit status is evidence only where the effect it claims can be seen. That control is what caught this section's own worst false green, where three comment-only files exited 0 and the gate read three successes.
- [x] 5.6 Falsifier 2 — `isolation.approval.sourceFingerprint` is taken over the file **as the human approved it**, so any later edit, the skill's own included, drops the approval and re-shows the template. *Verify: V63.*
  - **Done, with one correction to DS37 stated rather than absorbed: the fingerprint is taken over all THREE files, not over "the file".** DS37 says *the file as the human approved it*, written when the family was two files and the create was the only one an approval could sensibly be about. With three, a value taken over one of them leaves the other two editable under a consent nobody gave for them. It is one invocation of the same `sh scripts/fingerprint.sh` every other source fingerprint uses, over three paths in the order create, drop, read, so it is the same recipe rather than a second one; `discoveredFrom` still names the create, so the schema rule that an approval needs a file to expire it is unchanged.
  - **Measured on real files with the shipped script, not asserted.** Editing the create, the drop and the read each move the value; the value **returns to its approved figure** once every edit is reverted, which is what proves those three rows measured the edits rather than the reverts; and the skill's own later edit moves it exactly as the developer's does, asserted separately because it is the one a reader would assume is exempt. The negative that carries the correction: a fingerprint taken over the **create alone** does not move when the read is edited.
  - **The premise gate under all of it is the repair this task needed most.** `fingerprint.sh` emits `-` for a path it could not hash, three dashes hash to a valid object id, and appending to a file that does not exist creates it — so over a missing family every row above passed while comparing two absences. The block now requires every leg to digest to something that is not `-`, and reports `the approval fingerprint reads 3 undigestable leg(s) and hashed to '44e8570f…'` when they do not.
  - **The schema makes consent structural**: a `generated` record requires `approval`, and the existing rule then requires `discoveredFrom`. Both rejections are separate rows because they fail for different reasons, and stripping the consent rule sends `three files written into a repository with no approval recorded beside them` red.
- [x] 5.7 The generated **read** is held to DS34's discriminator like any other candidate: it qualifies only once its output is shown to differ against an empty instance of the same image. **A generated `SELECT 1` fails that test and must, exactly as `pg_isready` does.** Generate a schema-agnostic read — something that counts what an instance carries rather than naming a table the repository owns, so an empty instance answers zero and a seeded one does not — because the skill has no schema and carries no query table. The agent writes an engine-appropriate probe **once, at generation time, with the human approving it**, and from then on the file is the repository's. *Verify: V50; V49.*
  - **Done, and V50's generation half is RUN against real instances rather than reasoned about.** A generated read is written into the fixture repository, a real copy is provisioned from a seeded volume, an empty instance of the same image is started, and the one generated file is issued against all three from the host: `base '3', empty '0', copy '3'` — it discriminates, the copy matches the base byte for byte, and the pair is isolated. Beside it, a generated read of the `SELECT 1` shape, measured: `rejected as a query: a generated read measured answering '1' on an empty instance and on the base store alike`.
  - **The reader is 4b's `readback`, reused rather than copied**, so the generated candidate is judged by the same three-output, two-comparison mechanism a harvested one is. The only thing this slice adds is the door the skill could have let itself through, and it is proved by opening it: an admission path carrying an exemption for the skill's own authorship admits the constant read, and the row catches it — *declared-on-write reaching through the verification door*. A third row asserts the two origins reach the **same verdict on the same three outputs**, which is what "no exemption" means once it stops being a sentence.
  - **A route question DS34 does not answer, decided here and stated rather than smoothed over.** A rung-1 candidate is an argv vector already inside the store's image; a rung-2 read is a file in the **repository**, which is not in that image. So the generated read is issued **on the host** and is given the instance to read as its one argument. That is a different route in kind, and the invariant both are held to is the same one — all three issues go through one route, because a candidate issued one way here and another way there manufactures the difference it is being read for. It is stated in the generation section rather than added to `isolation-providers.md`'s exec-route table, which would have broken 4b's row asserting that table defines exactly one route.
  - **The admission rows stand down where the shipped rule does not point the read at the discriminator**, because the mechanism they exercise is 4b's and exists whatever this slice ships. Measured: unhooking that sentence gives `the shipped rule does not hold the generated read to the discriminator, so the admission rows would certify a mechanism nothing points the generated read at`. **The boundary is recorded rather than papered over**, as 4b recorded its own: what these rows prove is that the mechanism refuses what it should; what makes it the *generated* read's mechanism is the sentence.
- [x] 5.8 The generator's output is checked by the same contract as repository input, **with no exemption for being the skill's own**: the destructive-verb class judged by effect is applied to the drop before it is shown, and the recorded command invokes one program with arguments, satisfying the argv, deny-list and no-re-parse rules with no exemption. The teardown stays **inert until at least one run has observed that target's create succeed**; until then the run names the namespace and the command a human would run. *Verify: V63; V62.*
  - **Done, and the deny-list the generated drop is judged by is taken OUT OF the shipped template contract** rather than copied — the same list slice 4b reads for the harvest, so one shipped row governs both. Proved load-bearing by deleting `sh` from that row: the drop reader stops refusing the shell form and two of this section's rows go red, `the shell-wrapped drop was not refused by the re-parse rule` and `the deny-list fixture is not refused even with the shipped list`.
  - **The names a drop may remove are read out of the shipped rule too**, and asserted to be exactly the two family members. Four cases measured: the family member accepted — the answer without which the three refusals mean nothing — a literal name refused, `{{store}}` refused because it is a member of the closed set **and the base stack's own namespace**, and the shell-wrapped form refused **by the re-parse rule, naming it**. Dropping the family clause admits the literal; an empty deny list admits `sh -c {{isolationIdent}}`, whose only defect is its program.
  - **The teardown's inertness is a clause of the gate rather than a sentence beside it.** `- - -` answers `inferred inert`; an observed create makes it `runnable`; a create that ran and **failed** leaves it `inert`. Dropping the inert clause lets a teardown run before any create was seen to succeed, and the row catches it.
- [x] 5.9 Raise the four version numbers to `2.0.0` — `SKILL.md` `version`, `metadata.version`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` — and add the breaking `CHANGELOG.md` entry naming the `schemaVersion` 2 → 3 bump, the absent migration path as the design, and the two things that do not revert themselves: a `3` manifest is unrecognised by `1.1.0` and is discarded, and any store copy or approved lifecycle target created while 2.0 was live stays where it is. *Verify: V69.*
  - **Done. All four numbers move together** — `one version in four places: plugin.json=2.0.0 marketplace.json=2.0.0 SKILL.md=2.0.0 SKILL.md/metadata=2.0.0` — with the `2.0.0` entry and its link-reference definition both present, so the version renders as a link rather than as literal brackets, which is the asymmetry `1.1.0` shipped in. The existing partial-bump fixtures are the negatives and are untouched.
  - **V69 gains what a MAJOR release owes, which the shipped rows did not check.** An entry existing is not an entry that says the schema broke: the row requires the released version's body to carry `breaking`, `schemaVersion`, `no migration path`, `discard`, and `stays where it is`, with a deletion fixture on the residual sentence as its negative. It is **keyed on the major component rather than on the literal 2.0.0**, so `3.0.0` inherits the obligation instead of walking out from under it, and it stands down loudly on a minor release.
  - **Frontmatter only, and the body is sealed rather than merely unchanged in count.** Body **487** words before and after, `compatibility` **494** bytes, `description` **215** characters, identical from the primary checkout and from the linked worktree. `SKILL.md`'s diff against the tracker is 2 additions and 2 deletions, both of them a version string.
- [x] 5.10 Extend `.github/scripts/verify.sh` with V50's generation half, V62, V63 and V69; re-run the body counter and assert 4b.8's figure unchanged — the version bump is frontmatter, which the counter does not see. *Verify: run `sh .github/scripts/verify.sh` on macOS and `alpine/git`; V34.*
  - **Done. macOS: 718 ok, 0 FAIL, exit 0** (baseline at `a1fdf12` was 579 ok / 0 FAIL), identical from the primary checkout **and** from a linked worktree at `~/Workspaces/stackgraft-worktrees/pfi-5`, removed after, with the three counters identical there — body 487, `compatibility` 494 bytes, `description` 215 characters. **alpine/git: 489 ok, 3 FAIL, and those three are pre-existing** — `report under a held lock`, `an empty registry`, `a damaged registry` fail identically at `a1fdf12` from a clean clone in the same image (371 ok / 3 FAIL), so this slice adds no fourth. The new section contributes **116 green rows under alpine**; its runtime rows skip loudly there for want of a daemon, as the shipped container rows already do. `dash -n` clean on all five scripts.
  - **The suite's leak: this section's share is zero, and it is asserted by its own row** — `this section left no labelled object, no fixture, no probe and no unnamed volume behind` — with a positive control planting an object that carries this run's label and neither fixture name, to prove the envelope can see the one kind of leak a name filter cannot. `docker ps -a` and `docker volume ls` were diffed against a pre-work snapshot and restored to it exactly: 31 containers and 5578 volumes, byte-identical. The 380 volumes 21 full runs added were **all anonymous, all unlabelled, none named `sg-*`** — the pre-existing 18-per-run leak slice 4a measured and 4b confirmed, at 18 per run, and they were removed rather than left on the machine.
  - **A finding about the suite's teardown, recorded for the chain close rather than repaired from here**, because it belongs to slice 4a's fixture and 4a took the same posture with the 18 volumes. The provider section's part-way-failure fixture uses `docker create`, which leaves a container in `Created` **holding its volume**, and a run interrupted between that line and its removal leaves both behind — `docker volume rm` then answers *volume is in use* and a naive cleanup leaves the volume too. It was reached for real when a run of these demonstrations was killed at a timeout: the next run's inventory rows correctly refused to trust themselves with a foreign `sg-verify-src` present, which is the harness working. **This section's own teardown is written not to have that property**: containers before volumes, and by name unconditionally, so a `docker run -d` that fails after the runtime registered the name cannot leave one behind under an empty variable.
  - **Changed lines 1221** (1213 additions, 8 deletions) against a ~250 forecast and against the **~1000 slice 4b said to plan for** — **4.9× the original and 1.22× the revised**, and the first revised forecast in this change to land inside 25%. Reported rather than absorbed, and no assertion was compressed to reach a number. Where it went: **958** in `verify.sh`, **101** in `check_schema.py`, **67** in the schema, **48** in `shared-state.md`, **32** in `CHANGELOG.md`, and **8** across the four version numbers, `README.md` and `isolation-providers.md`. **The shipped skill prose is 51 of the 1221.**
  - **What drove it is one step past 4b's, and it is the hazard class this slice introduced.** 4a's subject was a mutation of the host; 4b's was a reading over runtime outputs. **This slice's subject is a mutation of a repository that is not this one**, and no reading can check it: an appended `Makefile` recipe is invisible to every grep and every reader in the file, so the only instrument that can see it is a hash of the bytes before and after. That fixture repository — built, committed, written into by a writer with four defect modes, four untouchable files digested, an approval fingerprint over three real files, and its own removal asserted — is ~330 of the 958. **The second driver is the one worth carrying to the chain close: the harness's own false greens cost more than the rules did.** Ten were found by measuring the red run, and every repair adds lines that assert nothing new — premise gates, positive controls, stand-downs, and six C1 extractions that exist only so a reader cannot supply the shipped rule it is judging.

## Phase 6: Chain close and pre-archive

- [x] 6.1 Re-run every success criterion in `proposal.md` against the tracker branch, criterion by criterion, with the row that carries each. A criterion no row covers is executed by hand and says so; nothing is marked met on inspection alone. Cover: a frontend-only diff gating zero pairs and making zero copies; a one-store writer copying one store and leaving three untouched; F1, F2, F4 and F5 each with a named regression case that fails against `1.1.0`; a MinIO bucket created under a name the substrate accepts; an X-only pair resolved by identity and never by copy; a W pair verified by a real query before it counts as isolated; a managed store refused by name with no partial isolation; a store with no record refusing in **every** mode including manifest-less; `dependsOn` still narrowing nothing; every copy labelled and reaped, no unlabelled volume ever a target; each copy's size and elapsed time reported and no duration predicted; the six no-safe-answer cases refusing unchanged; `schemaVersion` 3 with an unrecognised version still discarding; the body inside the ceiling by the counter; the provider passing `dash -n` and running on macOS and a minimal Linux image; the four version numbers at 2.0.0. *Verify: V1 + V2 + V3 + V4 + V5 + V34 + V36 + V37 + V69.*
  - **Done, and three of the sixteen do not close as written. They are reported open rather than ticked, because the thing that makes this table worth writing is that a criterion nobody could evidence looks exactly like one nobody checked.** Run against `2b47cc4` on the tracker chain. Suite figures: **macOS 736 ok / 0 FAIL from the primary checkout and 736 ok / 0 FAIL from a linked worktree** at `~/Workspaces/stackgraft-worktrees/pfi-chain-close`, removed after; **`alpine/git` 507 ok / 0 FAIL / 12 skip**. Counters at the tip: body **487**, `compatibility` **494** bytes, `description` **215** characters, each asserted as a literal beside its ceiling.

    | # | Success criterion | How it was evidenced | Verdict |
    |---|---|---|---|
    | 1 | A frontend-only change gates zero store pairs and makes zero copies | `a frontend-only diff gates 0 of 9 derived pairs`, and `a pair carrying X and not W is resolved by identity and leaves the runtime inventory unchanged` — the second proves the run made nothing, against a runtime inventory the same section shows able to move | **Met on the fixture repository. OPEN on the 43-service one**, which is not in this tree (decision-state item 5b) |
    | 2 | A one-store writer copies that store only; the other three are untouched and unread | `rejected: a narrowing with no record for that pair, and none on a sibling or another unit`, `(catalog-api, events) has no record and stays: undetermined`, `rejected: dropping the this pair clause — a sibling's record answers a pair nobody examined` | **Met on the fixture repository. OPEN on the 43-service one** — "the other three" is a property of that repository's shape |
    | 3 | Each of F1–F5 has a named regression case that fails against `1.1.0` and passes at 2.0 | F1 by hand against a real object store, below. F2: `no shipped file describes an allocation, a pool, an exhaustion case or a release obligation for a namespace name`, negative `rejected: a row handing out one of sixteen logical databases per worktree`. F4: `rejected by /properties/services/additionalProperties/properties/writes/not: a service-level writes array`. F5: `rejected by …/migrates/type: migrates as the unit-level boolean it used to be` | **Closes for F1, F2, F4, F5. F3 is D9, which Q7 puts outside this change** — deferred by a locked answer, not missed (task 6.3a) |
    | 4 | A MinIO bucket is created under a name the substrate accepts | Hand-run at this commit, MinIO in a throwaway container, both forms derived for `chore/pfi-chain-close`: `mc mb m/sg_chore_pfi_chain_close_7cdb59d1` ⇒ `mc: <ERROR> Unable to make bucket … Bucket name contains invalid characters`; `mc mb m/sg-chore-pfi-chain-close-7cdb59d1` ⇒ `Bucket created successfully`. Container and network removed, inventory unchanged | **Met**, and it is F1's regression case: every bucket `1.1.0` could name, rejected |
    | 5 | A coordination hazard with no data hazard resolves by identity, never by copy | `a pair carrying X and not W is resolved by identity and leaves the runtime inventory unchanged`, with the W-and-X pair's diff required to show a provision so the row is shown able to see one | **Met** |
    | 6 | A data hazard resolves by copy, verified by a real query before it counts as isolated | `a candidate that counts what the instance carries is a query, and the copy answering the base store's answer is isolated`; `rejected as a query: a candidate measured answering 'PONG' on the empty instance and on the base store alike`; `rejected: a truncated copy — it does not answer the base store's answer, so it is destroyed and the pair refuses` | **Met**, against real runtime objects |
    | 7 | A managed or remote store refuses with the reason named and no partial isolation attempted | `the store with no local state refuses by name and the local pair beside it still provisions — the refusal does not cascade`; `isolation-providers.md refuses managed, remote, host-native and undeterminable locality, each by name`; `rejected: a provision that failed part-way — its partial volume is gone and the inventory is identical` | **Met** |
    | 8 | A store with no per-store record is undetermined and refuses, in **every** mode including manifest-less | The record half is executed: `a store recording neither form is undetermined, which refuses`, `(catalog-api, events) has no record and stays: undetermined`. **The manifest-less half is file review only** — `references/shared-state.md:15` states *a run with no manifest owes exactly the same verdicts*, and no row in the suite runs the gate manifest-less | **Half executed, half reviewed. Stated rather than ticked**, because a documented equivalence is not an exercised one |
    | 9 | `dependsOn` still cannot narrow a pair set; only an expiring per-store record can | `rejected: dependsOn: [] removes no pair — every backingStores entry still yields one`; `the dependsOn prohibition is restated INSIDE the rule, in those words`, with `rejected: the narrowing rule with the dependsOn prohibition deleted` as its negative | **Met** |
    | 10 | Every copy carries the ownership labels and is reaped; no unlabelled volume is ever a target | `the copy carries all four ownership labels with this repository's hash8`; `rejected: a copy carrying a partial label set is reachable by no scoped query`; `rejected: an unlabelled volume holding data, untouched by a destroy that removed this run's own copy` | **Met** |
    | 11 | The run reports each copy, its size and its elapsed time, and predicts none | `address emits only record kinds the contract defines` over a live provision emitting `bytes` and `seconds`; `no shipped file states an expected, typical, or worst-case duration`, with `rejected: all four shapes of a predicted duration` | **Met** |
    | 12 | The six cases with no safe answer still refuse, unchanged | `the six cases with no safe answer are still six` and `each of the six cases is word-for-word what 1.1.0 shipped`, the literals taken from `v1.1.0` | **Met** |
    | 13 | `schemaVersion` is 3 and an unrecognised version still discards and rediscovers | `the schema declares schemaVersion const 3`; `schemaVersion is declared exactly once, so the chain carries one transition`; `rejected by /properties/schemaVersion/const: a manifest still written at schemaVersion 2`. **The discard-and-rediscover behaviour is a documented rule and not an executable row** — this repository ships no loader to run it against | **Version half met. Discard half reviewed**, and said so rather than ticked |
    | 14 | Body within the measured ceiling, counted by the command in `openspec/config.yaml`, never by eye | `body is 487 words (ceiling 500)` and `body is the 487 words this slice measured and recorded`, with the one-word drift fixture as its negative | **Met** |
    | 15 | The provider passes `dash -n` and runs on macOS and a minimal Linux image | `provider-docker.sh parses under dash` on macOS and `parses under sh` on `alpine/git`; `provider-docker.sh names no tool absent from the supported floor`; provision → address → destroy executed for real on macOS against a live daemon. **The provider's runtime rows SKIP inside `alpine/git`** — there is no daemon in that container — so what the minimal image proves is that it parses and names nothing absent there, not that it ran there | **Parse and portability met on both. Execution met on macOS only**, stated rather than implied |
    | 16 | The four version numbers agree at 2.0.0 and `CHANGELOG.md` carries a breaking-change entry | `one version in four places: plugin.json=2.0.0 marketplace.json=2.0.0 SKILL.md=2.0.0 SKILL.md/metadata=2.0.0`; `the 2.0.0 entry is marked breaking and states the schemaVersion bump, the absent migration path, the discard, and what stays where it is`; negative `rejected: each of the four places bumped on its own` | **Met. Not tagged and not released** — that is the owner's, deliberately |

  - **Three criteria stay open and the reason is the same one for all three: the repository this change exists for is not in this tree.** Criteria 1 and 2 name the 43-service repository by construction, and the 116/156 refusal baseline and the 39/4/156 derivation counts come from it (task 6.3c). Everything the suite can hold them to is held: the narrowing is executed against a multi-store fixture, the counts are asserted derived-beside-narrowed, and two runs at one commit are required to report identical counts. What is not evidenced here is the *number* those rules produce on that repository, and no row in this tree can produce it.
  - **Criteria 8, 13 and 15 are each half executed and half reviewed, and the halves are named above rather than averaged into a tick.** This is the same posture task 1a.11's Status rule takes: a claim about a mechanism that exists is worth exactly what was run against it.
- [x] 6.2 **Blocks archive, not apply.** Confirm the two spec sentences decision-state item 4 carries were corrected before their slices rather than after: `shared-state-safety`'s Verify clause narrowed from *the substrate namespace table is gone* to the **In-instance isolation column**, and `isolation-providers`' IP-5 reworded from *refused before it starts* to a **candidate, not a guarantee**. Archiving with the uncorrected sentences merges a contradiction into the baseline, which is what `overlay-reaping` tasks 3.3–3.5 spent three amendments unwinding. *Verify: file review — one rule, one outcome, in the spec, the design and the shipped file alike.*
  - **Confirmed, and both were corrected BEFORE the slice that implemented them rather than after, so the spec and the shipped file never disagreed for a commit.**
  - **(a) `shared-state-safety`, narrowed at slice 3 in commit `3ebe911`, before task 3.3 edited a byte.** The Verify clause now reads *the **In-instance isolation column** of the per-substrate table is gone while the table and its catch column remain*, and it carries the reason beside it: the four refuse-cases that rest on the catch column are named, so a reader who deletes the table deletes their evidence. The shipped file agrees — 14 substrate rows survive, the table is asserted **two columns wide**, and both directions have a negative (`the per-substrate table still carries the In-instance isolation column`; `the per-substrate table is 3 columns wide, so a third column came back under another name`).
  - **(b) `isolation-providers`' IP-6, reworded at slice 4a in commit `c107f89`, before task 4a.4 edited a byte.** It now reads *Free space is measured on the filesystem the copy will occupy, and reported as a candidate*, obliges the run to print its blind spot and name the binding filesystem, forbids *enough space* / *guaranteed* / *reserved* / *will fit*, and adds the mid-copy exhaustion that leaves the inventory as it found it. The shipped file agrees: `isolation-providers.md says what the check produces: a candidate, not a guarantee`, and the three wording fixtures are each rejected.
  - **One rule, one outcome, in all three places — checked at this commit rather than trusted from the slice notes.** `design.md`'s *Locked decisions this design cannot deliver as written* items 2 and 3 state both corrections; the delta specs carry the corrected sentences; the shipped files carry the behaviour with a negative each. Both old sentences survive in the tree in exactly four places and every one of them is a **record of the correction** rather than an operative rule: `design.md`'s locked-decisions items 2 and 3, decision-state item 4 in this file, task 3.3's and 4a.4's own text, and the correction note inside `specs/shared-state-safety/spec.md` that names what the clause previously read. Not one of them states the old rule as current, which is the distinction `overlay-reaping` tasks 3.3–3.5 spent three amendments learning to make.
- [x] 6.3 **Blocks archive, not apply.** Record the items that cannot be closed as written, per decision-state item 5, rather than ticking them. **(a) F3 has no regression case at 2.0** — it is D9, which Q7 places outside this change; the F1–F5 criterion closes for four of five and the fifth is deferred by a locked answer, not missed. **(b) `portable-runtime`'s "the per-substrate namespace table MUST leave the body entirely" is false as written** — that table has never been in the body; it is in `references/shared-state.md`, and the body's only isolation content is one Output Contract bullet plus the sealed Hard Rule 8. The net-negative obligation still closes (DS33), out of unrelated compaction, so the **requirement text must be corrected rather than the donor invented**. **(c) The 116/156 refusal baseline and the 39/4/156 derivation counts require the external 43-service repository** and cannot be CI rows; they are hand-run observations recorded with their commands. *Verify: file review that each is stated in the tree, not merely in this file.*
  - **All three recorded, and (b) is corrected here rather than carried further — the requirement text was the thing that was wrong.**
  - **(a) F3 has no regression case at 2.0, and that is a locked answer rather than a gap.** F3 is D9 — a credential channel for verification — and Q7 places D9 outside this change. The F1–F5 criterion therefore closes for **four of five**, which the criterion table in task 6.1 states in the row rather than in a footnote. F1 is measured against a real object store, F2 against the deleted allocator vocabulary, F4 against the retired service-level `writes`, F5 against the retired unit-level `migrates` boolean. F3 is deferred, and the criterion as written cannot close until D9 lands.
  - **(b) `portable-runtime`'s "the per-substrate namespace table MUST leave the body entirely" is false as written, and the REQUIREMENT is corrected rather than the donor invented** — that table has never been in the body. It is in `references/shared-state.md`, and the body's only isolation content is one Output Contract bullet plus the sealed Hard Rule 8. DS33 found this and closed the net-negative obligation out of unrelated compaction instead; the measured endpoint is **498 → 487**, net −11 with thirteen words of headroom, and no intermediate slice above the 1a figure of 484. Writing a donor that does not exist into a requirement is how a check comes to measure a thing nobody shipped. **Corrected in `specs/portable-runtime/spec.md` at this slice**, in the same shape the other two corrections took: the clause now says the table and the provider contract MUST both stay *out* of the body and the addition is paid out of unrelated compaction, with the previous wording and the reason recorded beside it. The obligation is unchanged; only the mechanism the sentence named is.
  - **(c) The 116/156 refusal baseline and the 39/4/156 derivation counts cannot be CI rows and are not asserted green by the suite.** They require the external 43-service repository, which is not in this tree. They are hand-run observations, recorded with the command that produces them, and criteria 1 and 2 in task 6.1's table are marked OPEN on that repository for the same reason. What the suite does hold is the *rule* rather than the number: the derived count is reported beside the narrowed one, a report that replaces one with the other fails, and two runs at one commit with one manifest state must report identical counts.
- [x] 6.4 **Blocks archive, not apply.** Carry `design.md`'s three non-blocking open questions into `references/traps.md` or `isolation-providers.md` as stated assumptions rather than resolving them silently: whether the cache directory's filesystem is the one backing the runtime's disk image on every supported platform (wrong only makes the host figure conservative in one direction, and the run says which filesystem it measured); whether a store image without a shell can host the copy container (where it cannot, the provider refuses and names it); and `podman`/`nerdctl` volume-and-label parity (a wrong guess produces a refusal, not an unlabelled copy). *Verify: file review — each is stated with the direction it fails in.*
  - **Done, in `references/isolation-providers.md`, and the file review is mechanised because a file review is what let two of the three go unstated for four slices.** Measured before writing: only the cache-directory one was in a shipped file at all, and it named the assumption without naming which way it breaks; the distroless question and the `podman`/`nerdctl` question were in `design.md` and nowhere else.
  - **Each row greps the DIRECTION clause rather than the topic**, so an assumption stated without its failure mode does not satisfy its row. An assumption whose failure mode is unstated is a claim, and this is the difference between the two in one grep. RED demonstrated with real output before the sentences existed: `isolation-providers.md never states: the cache-directory assumption fails conservatively, never permissively`, `… a store image that cannot host the copy refuses, rather than yielding an unverifiable copy`, `… a runtime that does not share Docker's volume and label vocabulary produces a refusal` — three FAILs, then green.
  - **The cache directory's filesystem**: assumed to back the runtime's disk image, and where it is wrong the two figures are still measured separately and the smaller binds — so the error makes the check **more** conservative, never more permissive, and the run names the filesystem it measured.
  - **A store image without a shell**: the copy runs the store's own image, so a distroless build may not host the copy step. The provider refuses and names the store; the failure direction is a refusal, never a copy that was taken and cannot be verified.
  - **`podman` and `nerdctl` parity**: only the Docker provider ships and only Docker is measured. A wrong guess errors and refuses rather than producing an unlabelled copy, because every label is supplied at creation and a creation that did not take them is a creation that failed.
- [x] 6.5 **Blocks archive, not apply.** Reconcile the capability-directory set before the merge: this change's seven delta capabilities against the six in `openspec/specs/` — `manifest-contract`, `orphan-reclamation`, `overlay-ownership`, `portable-runtime`, `shared-state-safety`, `topology-discovery` — so `isolation-providers` and `coordination-identity` land as the two new ones and no delta merges a second directory covering a file an existing capability already owns. `overlay-ownership` is consumed by this change and not restated; confirm the archive does not create a competing copy of it. *Verify: file review — `openspec/specs/` gains exactly two capability directories and loses none.*
  - **Reconciled. `openspec/specs/` gains exactly two and loses none: 6 → 8.** The seven delta directories are `coordination-identity`, `isolation-providers`, `manifest-contract`, `orphan-reclamation`, `portable-runtime`, `shared-state-safety`, `topology-discovery`. Five of the seven carry a baseline directory's **exact** name; the two that do not are the two new ones. Baseline `overlay-ownership` appears in no delta, so the archive merges nothing into it and creates no second copy of it.
  - **Corrected count, recorded rather than absorbed: this change modifies FIVE capabilities, not four.** `proposal.md`'s *Modified* list names `shared-state-safety`, `manifest-contract`, `topology-discovery`, `portable-runtime` and `orphan-reclamation`, and the delta directory holds all five. Two new plus five modified is the seven the tasks header states. Anywhere the chain says four, the directory listing governs — the same posture decision-state item 3 took when DS33's rows and its subtotal disagreed.
  - **The collision class that bit `overlay-reaping` was looked for by name and is not here.** That change shipped a `manifest-cache` directory while the baseline held `manifest-contract` — two capability names covering one subject, which had to be settled before either could archive. The two candidates in this change were checked against the baseline they could collide with:
    - **`isolation-providers` against `overlay-ownership`.** Different objects, not two names for one. `overlay-ownership` governs **container-kind overlays** and the five labels a launch supplies (`labels`, `repo`, `worktree`, `service`, `port`); IP-8 governs **volume copies** and the four a copy carries (`labels`, `repo`, `worktree`, `store`). IP-8 cross-references `../orphan-reclamation/spec.md` for the never-a-target rule instead of restating it, which is the shape a split takes when it is a split.
    - **`coordination-identity` against `shared-state-safety`.** A deliberate split with the boundary written into the spec rather than left to a reader: the delta's *Competitive attachment* requirement carries `(Verify: file review that the X procedure lives in ../coordination-identity/spec.md and that references/shared-state.md links rather than restates it …)`, and the suite executes it — `the identity proof has exactly one home, and it is coordination-identity.md`, with `rejected: shared-state.md with 1.1.0's identity procedure restated beside the link` as the negative, and `proof_legs` measuring **0** legs in `shared-state.md` and **3** in the new file.
  - **Both RENAMED requirements are handled inside their own capability directory rather than by a second directory**, which is the mechanism that would have produced the collision: `manifest-contract`'s *Schema v2 field contract → Schema v3 field contract*, and `shared-state-safety`'s *Isolation reuses the server, never the namespace → ISOLATE means a seeded copy, and in-instance isolation is the optimisation*. Each carries its Reason and Migration lines, so the archive has one instruction per rename and no second home to merge.

### Slice 6 close: the five carried debts, and what measuring them found

Every debt below was recorded by the slice that created it and left for this close. All five are repaired here; two of them turned out to have a source nobody had attributed, and both are stated with the measurement rather than folded into the fix.

- **Debt 1 — the suite leaked 18 anonymous volumes per run. Fixed at source, and the run now asserts zero.** `alpine/git`, the fixture image every runtime row starts, declares `VOLUME /git` in its own Dockerfile, so every container of it gets an unnamed volume whether or not the launch asked for one. Traced with a `docker` shim recording each container's mounts at creation: **exactly four call sites, 18 volumes, and the attribution is complete** — 14 from the reap fixtures' `docker rm -f "$id"`, 2 from the two-repo scoping fixture, 1 from the held-port fixture, and **1 from a container started with `--rm`**, where `docker rm -f` beats the daemon's own autoremove and the volume is left behind anyway. All four now carry `-v`. Asserted by a run-wide ledger: every container registers its unnamed volumes at creation, and one row requires **26 registered, 0 surviving**. Keyed to this run's own containers rather than to a whole-runtime diff, because a developer's other stack starting a container mid-run would read as this suite's leak in the second shape and cannot in the first. The paired negative builds the leak deliberately and requires the ledger to report it. **Measured across the chain close: host volume delta 0, container delta 0, on every run from here on.**
  - **A nineteenth was found by the ledger and it was not the harness's: `reap.sh` removed a labelled overlay with `docker rm -f`.** The reaper orphaned the container's anonymous volume — an object with no name, no label and no owner, which is the exact class this script exists to reclaim, in the one form it could never afterwards find. Repaired in the shipped script and in `references/reaping.md`'s flags table together, because repairing one alone is the two-texts hazard. **The safety premise is measured rather than cited**: on Docker 29.5.3, against a container mounting one named volume and carrying one of the image's own, `docker rm -f -v` removes the anonymous volume and leaves the named one — which is what makes the flag safe here, since a base stack's data is named and so is every copy this skill provisions. That row ships with the fix.
- **Debt 2 — `verify.sh`'s `docker create --name` fixture could leave a `Created` container holding its volume. Teardown ordering repaired in both sections that lacked it.** A container in `Created` holds a volume exactly as a running one does, so `docker volume rm` answers *volume is in use* and a teardown reaching for the volume first leaves **both** behind. The provider and verification sections now tear down **containers before volumes, by name, unconditionally**, the shape the generated-target section already carried: the planted name is handed to the teardown before the container exists, and the removals no longer live only on the path that reached them.
- **Debt 3 — `SECURITY.md:21` described a closed placeholder set of five naming the retired `{{isolationName}}`. Corrected, and the sweep widened to the surface it should have covered.** Slice 3 retired the placeholder and swept `skills/stackgraft README.md docs/`; `SECURITY.md` sits outside that list, so for three slices the project's security page documented a placeholder no code substitutes and no schema accepts — **two lines, not one**: line 21's set, and line 27's destructive-verb rule deriving from it. The shipped surface is now named **once** and three sweeps share it (`SHIPPED_SURFACE`), with `SECURITY.md` and `CONTRIBUTING.md` in it, an inventory row so a renamed path fails rather than shrinking every sweep, and a row asserting `SECURITY.md` is in the list **by name**. `CHANGELOG.md` is deliberately excluded: its breaking entry says what `1.1.0`'s `{{isolationName}}` could not name, and that sentence is true. **RED with real output before the correction**: `2 line(s) still use the retired {{isolationName}}: SECURITY.md:21:- **A closed placeholder set of five** …`.
- **Debt 4 — `openspec/config.yaml` still prescribed the retired `compatibility` counter. Replaced, and the file is now held to naming the counter that runs.** It recorded `awk -F'"' '/^compatibility:/{print length($2)}'` — the counter `compat_measure()` replaced because it printed nothing for an absent field, `${compat:-0}` read `0`, and `0` was under the ceiling, so **deleting the field passed the check outright**. A project whose recorded verification method is a check that cannot fail is telling the next reader to reproduce the defect. The entry now names `compat_measure()`, states why the field split is retired, and a row requires both — with a negative that reinstates the expression on a scratch copy.
- **Debt 5 — the three `alpine/git` failures were fixture path-sensitivity, and they are fixed rather than explained away.** `reap.sh` derives the registry filename from the **main worktree's directory name**; the fixtures planted `stackgraft-<hash8>.processes.json`. Inside the container the tree is mounted at `/w`, so `sidecar_path()` looked for `w-00c0ffee.processes.json` and reported `registry-missing` — measured directly: `common=.git  main=/w  basename=w`. The three rows therefore failed on a minimal Linux image at **every commit including the baseline**, and the diagnosis was the fixture both times somebody looked. Each block now brings **its own fixture repository**, whose directory name it chooses, so one arrangement covers a primary checkout, a linked worktree and a bind mount — the same repair the minimal-image script rows took. The name is **not** derived from `reap.sh`'s own expression, because a fixture that computes the answer cannot notice the answer changing: the report prints the file it read, and a new premise row requires that to be the file the block planted. **`alpine/git` is now 507 ok / 0 FAIL.**

**Two findings carried out of this slice rather than fixed in it, with their measurements, the same posture task 3.4 took with `SECURITY.md` and 4a took with the eighteen volumes.**

1. **`scripts/provider-docker.sh` line 421 removes the copy volume without removing the container that may hold it, and then reports a removal that did not happen — carried here, and FIXED afterwards on `fix/provider-refusal-teardown`.** On the instance-start failure path it runs `docker volume rm "$name"` and refuses with *the instance would not start on the copy, and the copy was removed*. Reachable whenever a previous run of the same `(repo, worktree, store)` left its container behind — which is exactly the state the part-way-failure fixture models, one object short. Measured: with a container named `sgprov-x` holding volume `sgprov-x`, `docker run -d --name sgprov-x` answers `Conflict. The container name "/sgprov-x" is already in use`, `docker volume rm sgprov-x` answers `remove sgprov-x: volume is in use - [0e5707a909c7…]`, and **the volume survives while the run says it was removed**. `references/isolation-providers.md` already states the rule this path breaks — *Order | Instance first, copy second. A copy still attached to a running instance is one the runtime refuses to remove, and reporting that as success is the half of the leak that matters.* **Not repaired here on purpose**: the fix has to decide whether a refusal may remove a container it did not create, and that is a design question rather than a chain-close correction. Opening it in the last commit of the chain is how a close becomes a slice.

   **What the repair found: the reachability recorded above is not reachable, and the design question does not arise.** A previous run leaving *both* objects behind is refused at the already-exists guard long before line 421 — `docker volume inspect "$name"` succeeds and the run answers *a copy named '…' already exists; destroy it first*. A previous run leaving *only the container* fails the name conflict **before** the container is created, so nothing holds the copy and the naive removal succeeds — which is exactly why the part-way-failure fixture passed over the defect for six slices. The reachable path is the other one: a `docker run` that fails **after** the runtime created the container, which leaves it in `Created` holding the copy. Measured against the shipped script with a base instance whose entrypoint lives on a volume of its own, so the copy is created with an entrypoint it does not have: `remove sg-deadbe02-40a73743-reprostore: volume is in use - [343e5542…]`, swallowed by the redirect, exit 3, *and the copy was removed* printed over a copy still on the disk — together with the anonymous volume the failed instance's image declared, a nineteenth source of the leak the close had just measured at eighteen. Because that container is the one **this run just named**, removing it is removing what this run made, and the question of removing a container it did not create is answered by not asking it. **What shipped:** all three provision paths that can leave a copy go through one `refuse_removing_copy`; the container goes first and with `-v`, and **only where it holds the copy**, so the orphan an earlier run left under the same name is untouched — proved by a counterfactual in which an unconditional removal destroys it; the copy is then **read back**, and where it survives the run **names it and exits 4** rather than reporting a removal. Six rows added, three of them without a runtime. macOS **742 ok / 0 FAIL** from both checkout kinds, `alpine/git` **510 ok / 0 FAIL / 12 skip**, body 487 / `compatibility` 494 / `description` 215 unmoved, docker inventory identical.
2. **The suite cannot be run inside a container against a bind-mounted LINKED worktree, and that is a property of git rather than of this change.** A linked worktree's `.git` is a file reading `gitdir: <absolute host path>`, and that path does not exist in the container: `git rev-parse --show-toplevel` answers `fatal: not a git repository: (null)` and everything downstream of git collapses. Measured at both ends — **453 ok / 33 FAIL at `fbadf8e`** and 469 ok / 28 FAIL on this branch, the difference being the three registry rows debt 5 repaired. The suite's own minimal-image rows are unaffected and assert this directly (`the minimal-image rows get a working git from a LINKED worktree too`), because `alpine_scripts` builds the container its own repository rather than mounting one. The `alpine/git` leg of this chain is therefore run from a **primary checkout**, which is what CI does; mounting the git common directory at its host path would fix it and is the arrangement `verify.sh` already rejects in writing, for two stated reasons.

**Changed lines 434** (404 additions, 30 deletions) against a forecast of *"a chain close writes no mechanism; its cost is re-running criteria by hand, not lines"* — **over the 400 guideline, and the seventh forecast in a row to come in low.** Reported rather than absorbed. Where it went: **408** in `verify.sh`, **15** in `reap.sh`, **11** in `isolation-providers.md` and `reaping.md`, **4** in `SECURITY.md`, **2** in `openspec/config.yaml`. **What drove it is that four of the five debts were not documentation.** The forecast assumed a close re-runs what six slices already built; three of the five debts turned out to need mechanism — a run-wide ledger with a registration at every container site, two teardowns rewritten, and three fixtures rebuilt around a repository they bring themselves — and the fourth, the leak, had a source in **shipped code** that only the new ledger could see. **The number to carry forward is not the line count but the shape: a debt recorded as "fix the fixture" is a forecast, and two of these five were measurements waiting to happen.** `SKILL.md` is untouched by this slice — it appears in no diffstat against the tracker, so the body is sealed rather than merely unchanged in count.
